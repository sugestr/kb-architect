# Механика изменения контракта — заметка владельца стандарта

> **Рабочей сессии это читать не нужно.** Здесь не правила хранения, а правила изменения
> правил: заморозка, гейт, определение дефекта, необратимый ярус. Сессия, ведущая проект,
> обходится контрактом и, при необходимости, паттернами.
>
> Раздел вытесняет вводную часть контракта: раньше она стояла перед рабочими правилами,
> и рабочая сессия была вынуждена читать теорию заморозки прежде, чем узнать, что делать.

**Контракт заморожен на время обкатки.** Проверки: через **7 дней активной работы** в проекте и через 30.

Это не следующая версия стандарта. Это **вычитание**: то, что осталось после того, как два инвертированных ревью и внешний рецензент независимо сказали, где ядро проходит их собственный тест цены, а где нет. Всё остальное в этом скилле — **справочник, а не контракт**: объяснения, паттерны, шаблоны и инструменты, к которым обращаются по надобности. Соблюдать надо только то, что ниже.

**Правило заморозки:** пока идёт обкатка, в этот раздел не добавляется ничего. Ни по какому ревью, включая то, которое окажется убедительным. Всё, что хочется добавить, пишется в `SLOMALOS.md` проекта и рассматривается на разборе — по факту, а не по аргументу.

**Исключение для необратимого яруса.** Правило, нарушение которого необратимо (ушло наружу, подписано, стёрто), проходит гейт в обратную сторону: **включается и ждёт опровержения**. Ставить эксперимент, сигналом успеха которого является утечка, недопустимо.

Чтобы исключение не поглотило гейт целиком — необратимым объявляется не неприятный исход, а **проверяемая причинная цепочка**, все шесть звеньев: существует конкретное действие · его может выполнить агент · после выполнения ошибка практически не устраняется · действующий контракт её не предотвращает · правило встаёт непосредственно перед точкой невозврата · более узкого ограждения с меньшей ценой нет. Не сходится хоть одно звено — обычная очередь.

И **статус временный, а не презумпция вечного включения**: на разборе такое правило оценивается не по «была ли утечка», а по числу наступивших точек применения, фактическому выполнению, ложным остановкам и возможности перенести его в шаблон или в разрешение инструмента. Иначе необратимый ярус становится новой дверью для роста.

**Симметрия правок.** Сокращение по аргументу — та же правка по аргументу, от которой защищает заморозка. Поэтому **и добавления, и сокращения идут через факты эксплуатации**, а не через убедительность: аргументы обеих направленностей складываются в отложенный список и достаются на разборе.

Исключение одно — **дефект**, и он определён узко, двумя проверяемыми категориями:

1. **внутреннее противоречие** — текст в одном месте требует того, что в другом запрещает;
2. **неполный механизм** — правило ссылается на проверку, которой в контракте нет.

Обе проверяются чтением, без суждения о том, как правильно. Формулировки вроде «это реализация, а не инвариант» сюда **не входят**: под них подводится почти любое правило, и это аргумент в костюме дефекта. Такие идут в отложенный список наравне с прочими.

Опровергать конструкцию можно в любой момент и без ограничений — опровержение не меняет текст само по себе.

---

---

## Три интерфейса, три аудитории

Смешение этих ролей и было главным источником ощущения сложности.

| Кто | Что читает |
|---|---|
| **рабочая сессия проекта** | контракт · правила проекта · при нужде — один паттерн |
| **архитектор конкретного проекта** | паттерны, профили, контуры, модули, измерение |
| **владелец стандарта** | эту заметку, очередь отложенного, обоснования, историю версий |

**Умирает, когда:** контракт перестанет меняться — тогда механика изменения не нужна,
и заметка удаляется целиком.

---

## Когда заморозка снимается

Заморозка без объявленного условия окончания становится постоянной по инерции — и превращается из механизма в церемонию. Условие объявляется здесь.

**Заморозка снимается, когда выполнены оба:**

1. **Три проекта наработали по контракту достаточный объём — считается интенсивностью, а не календарём**, и в их журналах есть записи обеих колонок, а не только «сломалось».

   Календарь здесь был ошибкой, и она противоречила соседнему разделу этого же файла: очередь правок давно считает **наступления триггера**, а не недели («триггер за период не наступал — период не засчитывается»). Разморозка считала месяцы. Два способа счёта в одном документе — ровно то, что запрещает первое правило.

   Месяц простоя не даёт ничего: база, к которой не обращались, не проверяет ни одного правила. Неделя интенсивной работы даёт больше, чем квартал спокойной. Поэтому единица — **сессия, которая действительно работала с базой**: прочитала вход, что-то записала, прогнала проверки.

   Порог по умолчанию, до появления данных получше: **не меньше двадцати таких сессий на проект и не меньше десяти записей в журнале, из них хотя бы три в колонке «сработало»**. Числа взяты с потолка и помечены как взятые с потолка — заменить их должен первый же разбор, у которого будут настоящие журналы. Считаются по коммитам базы и по самим записям: и то и другое уже ведётся, нового учёта не заводится.

   **То же относится к заморозке кода инструментов.** Объявлять её сроком — «неделя без правок» — значит мерить не тем: если за неделю никто не запускал проверки, неделя ничего не доказала. Окно закрывается тогда, когда инструменты отработали заявленный объём **на неизменных тестах**, а не когда прошло семь дней.
2. **Проведён разбор очереди** по этим журналам: каждый пункт получил решение «вносим / выбрасываем / ждём дальше» на основании записанных условий подтверждения и опровержения, а не аргументов.

После этого контракт правится обычным порядком: предложение → проверка на цену соблюдения → внесение с записью в таблицу выпусков. Очередь и гейт остаются, исчезает только правило «не трогать вообще».

**Чего заморозка не переживает как основания для снятия** — они перечислены явно, потому что каждое звучит убедительно в момент, когда возникает:

- сильный аргумент, даже безупречный;
- разрешение владельца (это снятие ограничения ровно тогда, когда ограничение работает);
- срочность («нужно сейчас, потом забудем»);
- накопившееся раздражение от того, что заморозка мешает.

**Почему вообще замораживали.** За сутки стандарт прошёл пять версий, вырос вдвое и не был применён ни дня: каждое возражение превращалось в новый механизм за часы. Артефакт, который любую критику конвертирует в следующую версию, невозможно опровергнуть — а значит, и проверить. Заморозка — не уважение к тексту, а способ заставить его столкнуться с реальностью прежде, чем он снова вырастет.

**Признак, что заморозка выродилась в церемонию:** за период она ни разу не изменила исход. Если каждое предложение, попавшее в очередь, в итоге вносилось без изменений, — гейт ничего не фильтрует и его стоит убрать. Проверяется на разборе, по журналу решений.

---

# Очередь правок контракта



Правки контракта — и добавления, и сокращения — вносятся по фактам эксплуатации, а не по убедительности аргумента. Здесь лежат аргументы, которые звучали сильно, но не подтверждены ничем, кроме рассуждения. Достаются на разборе и сверяются с журналом эксплуатации.

Формат: **что предлагается · кто предложил · какое наблюдение подтвердит · какое опровергнет.**

### Чего здесь не лежит: ошибки

Разница, которую легко потерять, и потеря дорогая — очередь становится местом, куда прячут известную поломку под видом осторожности.

**Ошибка стандарта чинится сразу, без очереди.** Внутреннее противоречие, устаревшая формулировка, дубль одного правила в двух местах, инструмент, который врёт или молчит там, где должен говорить, — здесь нечего подтверждать наблюдениями: неверное не станет верным от того, что подождёт месяц. Цена промедления вся на владельце, цена починки — одна правка.

**В очередь идёт не ошибка, а новая обязанность.** Предложение, которое добавляет проектам артефакт, поле или шаг, — даже безупречно обоснованное — стоит денег каждому проекту, который его возьмёт, включая те, где описанной проблемы нет. Наблюдение доказывает, что проблема настоящая; оно не доказывает, что предложенное решение работает. Разница между «ты видишь ошибку» и «ты видишь предложенное лекарство» — это и есть всё содержание очереди.

Проверка в одну фразу: **правка убирает неверное — чини сейчас; правка добавляет работу — в очередь.**

### Владелец может снять ожидание — и это не нарушение

Очередь защищает проекты от цены непроверенных решений. Владелец, который эту цену платит, вправе решить, что готов заплатить сейчас. Тогда:

- предложение ставится **опциональным модулем в справочник**, не в контракт: проект, у которого этой проблемы нет, ничего не наследует;
- у него записывается **дата пересмотра** и **условие отката** — что будет считаться признаком, что модуль не работает;
- в таблицу выпусков это пишется как решение владельца, а не как подтверждённое наблюдение. Иначе через месяц никто не отличит проверенное от принятого авансом.

Чего так делать нельзя: вносить в контракт. Обязательная страница — единственное, что наследуют все проекты сразу, и её заморозка держится не на осторожности, а на том, что артефакт, превращающий любую критику в новую версию, невозможно проверить.

### Разобран ли отчёт до конца

У отчёта нет состояния «закрыт», и это уже стоило: выпуск взял из отчёта два наблюдения из четырёх, оставшиеся два пролежали незакрытыми, и обнаружилось это случайно — при перечитывании отчёта по другому поводу.

Поэтому: разбирая отчёт, пройди **по всем** его наблюдениям и у каждого запиши исход — починено (в каком выпуске) · в очередь (с условиями) · не наш класс (чей). Отчёт без такой разметки считается неразобранным, сколько бы правок из него ни вышло.

---

## Сокращения

**Убрать `verify` из контракта в справочник.**
Аргумент: поле относится к утверждению, а не к файлу; в одном файле десять отправок с разными подтверждениями. Файловая метка тут заведомо грубее предмета.
Подтвердит: в журнале появляются записи «поле не к чему прицепить» или файлы с несколькими действиями и одним `verify`.
Опровергнет: записи «`verify` поймал расхождение» — то есть поле сработало в том виде, в каком оно есть.

**Убрать `valid_until` из контракта.**
Аргумент: для большинства утверждений автор **не знает** дату, до которой они останутся истинными; состояние живой системы меняется без предупреждения. Полезнее «когда наблюдали, против чего, какой объём», чем обещание про будущее.
Подтвердит: поле систематически не заполняется, либо заполняется наугад — датой «через месяц» без основания.
Опровергнет: записи «`valid_until` показал, что цифра просрочена».

**Убрать обязательность входа как файла (уже частично сделано) и вход целиком.**
Аргумент: в трёх из четырёх проектов «что дальше» естественнее вычисляется из реестра, а не пишется руками.
Подтвердит: вход руками не обновляется, а рядом живёт сборка, которой пользуются.
Опровергнет: вход обновляется и в журнале есть «посмотрел вход и не пошёл искать».

**Убрать `CORRECTIONS.md` в пользу реестра расхождений.**
Аргумент: однострочная запись не несёт влияния, владельца и статуса; append-only файл превращается во вторую базу известных ошибок, которую никто не применяет.
Подтвердит: записи копятся и не закрываются; или наоборот, канал пустой при живых расхождениях.
Опровергнет: записи появляются и закрываются, а исправления доходят до источника.

**Убрать правило «реорганизация только с согласия» из контракта в инструкцию по внедрению.**
Аргумент: это правило полномочий конкретного владельца, а не принцип хранения; в новом проекте оно создаёт лишние остановки и спор о том, что считать реорганизацией.
Подтвердит: записи о том, что согласование тормозило работу там, где ничего необратимого не происходило.
Опровергнет: случай, когда правило остановило переезд, который сломал бы ссылки.

**Сжать контракт до трёх утверждений:** не обслуживать один класс состояния из двух независимых источников · не утверждать внешнее состояние без сверки или оговорки · не править представление вместо источника. Плюс малый регрессионный тест.
Аргумент: это честный размер пересечения четырёх разных систем; остальное — уменьшенная копия архитектуры одного проекта.
Подтвердит: за период сработали только эти три, а прочие правила дали одни жалобы на цену.
Опровергнет: в журнале «сработало» есть записи по полям и по входу.

## Добавления

**Предусловия необратимого внешнего действия — вычисляемая карточка, а не правило.**
Предложил: отчёт кроссплатформенной сессии по инвестиционному проекту (одно наблюдение).
Аргумент: база **содержала** нужное знание — «аналитика без утверждённых налоговых параметров маркируется как доналоговая» — и оно **было прочитано**, но не стало блокирующим условием: сессия перешла к количествам и лимитным ценам продажи, не проверив налоговый возраст конкретных лотов. Ущерба не возникло, заявки не создавались. Класс знакомый: знание, прочитанное и не ставшее условием, — это ровно та ситуация, где соблюдать дороже, чем нарушать, и правило-абзац по критерию стандарта не годится. Форма, которая годится: **вычисляемый из канонических данных артефакт**, без которого действие не собирается, — карточка со статусом «проверено / не проверено».
Почему не вносится сейчас: это новая структура в проекте, и куплена она **одним** наблюдением, к тому же без ущерба. Стандарт про хранение знания, а не про исполнение действий, и граница проходит здесь — но с той стороны границы у него нет ни одного механизма, и это может оказаться настоящим пробелом, а не чужой территорией.
Подтвердит: второй случай в **другом** проекте, где записанное условие было прочитано и не применено перед необратимым действием; либо один случай с фактическим ущербом.
Опровергнет: проекты решают это доменными средствами (карточка в самом инвестиционном или юридическом слое) и в стандарте её не ищут; либо в журналах появляется «карточка заполнялась формально» — то есть она выродилась в декларацию, от которой её и пытались отличить.

**Класс, где доказательство есть, а способа перепроверить не существует.**
Предложил: медицинский архив, одно наблюдение.
Аргумент: `verify` означает способ перепроверки. Для утверждений «файл передан в соседний проект» и «закоммичено» доказательство есть — путь, хеш, — а перепроверить через внешнюю систему нечем: сосед может файл удалить, и поле останется зелёным. Поле вмещает доказательство и не вмещает способ, то есть в этом классе оно тихо меняет смысл на противоположный тому, что записано в контракте.
Почему не вносится сейчас: одно наблюдение, ущерба не было, и неясно, нужен ли отдельный класс или достаточно запретить `verify` там, где перепроверка невозможна, — второе дешевле и, возможно, достаточно.
Подтвердит: случай, где зелёный `verify` этого рода оказался ложным — сосед удалил, коммит переписан. Опровергнет: проекты и так не ставят `verify` на такие утверждения, и класс существует только в рассуждении.

**У утверждения не хранится адрес его источника.**
Предложил: проект ВНЖ, **пять наблюдений одного отчёта** (1, 2, 4, 6, 7) плюс независимо совпавший разбор автора.
Аргумент: база помнит, что она знает, и не помнит, откуда. Отсюда четыре разных на вид беды с одной причиной. Утверждение о содержании учредительного документа не хранило страницу — сессия спорила с владельцем и была неправа, а неверное значение уже стояло в двух подаваемых документах. Четыре файла с одной переписанной фразой сошли за четыре подтверждения: правило 1 запрещает два независимо поддерживаемых источника и не защищает от **умножения одного**. Отменяющий файл написал «заменяет предыдущие рабочие версии» без адресов — и отменённое число живёт дальше, причём в самом большом и самом связном файле папки, то есть выигрывает у канона на релевантности; разница трёхкратная и материальная. Проверенный первоисточник вернулся в базу именем без адреса, и его искали в сети заново.
Форма, которая напрашивается: обратное направление. Не `superseded_by` в отменённом (автор отмены не пойдёт искать все вхождения, иначе отмена не понадобилась бы), а **список адресов в каноническом файле: что именно он заменяет** — и проверка идёт от списка. Проверяемо механически и точно.
Почему не вносится сейчас: правка обязательной страницы, а её владелец отдельно не решал. Наблюдений хватает — их пять и они с ценой; недостаёт **решения**, а не доказательств.
Подтвердит: решение владельца. Опровергнет: попытка описать поле показывает, что оно требует от автора ровно той работы, которой он избегает, — тогда это дисциплина проекта, а не механизм стандарта.

**Пути, записанные инлайн-кодом, — проверять ли их как ссылки.**
Предложил: отчёт проекта ВНЖ, наблюдение 3 (одно наблюдение).
Аргумент: по смыслу это ссылка на файл базы, по форме — код, и разбор ссылок её не видит. «(`documents/`)» — самый частый способ сослаться на папку в русском техническом тексте.
Почему не вносится: **проверено и измерено, а не отклонено рассуждением.** Реализация с узнаванием «похоже на путь» дала на четырёх живых базах 72 находки при одном варианте условия и 312 при более широком; в выборке настоящих поломок почти не было — фигурные подстановки вида `{3,4,5}`, шаблонные заглушки `sources/otchet-YYYY.md`, ссылки скилла на собственные файлы из вложенного `SKILL.md` и записи канала правок о файлах, которых намеренно больше нет. Ложная тревога дешевле пропуска, но после второй раздел перестают читать — здесь их были бы десятки на каждой базе. Отдельно: **исходное наблюдение этой проверкой не ловится вовсе** — там инлайном названа существующая папка, а имя файла в тексте не прозвучало; ссылка без имени файла механически неотличима от упоминания папки.
Подтвердит: способ отличать ссылку от упоминания, который на этих же четырёх базах даёт единицы находок, а не десятки, — например, явная разметка ссылки в самом тексте. Тогда проверять станет что.
Опровергнет: второй отчёт, где инлайн-путь стоил чего-то заметного, — тогда цена шума пересчитывается против цены пропуска.

**Шаблон входа приглашает нарушать правило 1.**
Предложил: проект-recovery конфигурации двух ИИ-систем (одно наблюдение, двое суток расхождения).
Аргумент: контракт запрещает вторую независимую копию класса состояния — и тут же даёт форму «ГДЕ МЫ — три-пять предложений», которая физически не может не пересказывать канон. Пересказать дешевле, чем сослаться: критерий стандарта провален его же шаблоном. Предлагается строка о том, что «ГДЕ МЫ» ссылается на канон, а не пересказывает, с примером ссылки.
Почему не вносится сейчас: правка обязательной страницы, а наблюдение одно; форма изложения не проверяется механически, и есть риск получить декларацию.
Подтвердит: второй проект, где вход разошёлся с каноном пересказом, а не ошибкой. Опровергнет: проекты пишут «ГДЕ МЫ» ссылками сами, без указания.

**Контракт написан в единственном числе, а баз с двумя пишущими агентами всё больше.**
Предложил: тот же проект (Claude + Codex, общий репозиторий, работа передаётся в обе стороны).
Аргумент: «инвариант входа» у двух агентов фактически два — свой файл входа у каждой платформы; «довести до общего канона» не следует ни из одного из четырёх правил, и незапушенная работа для второй системы не существует. Recovery-план двое суток восстанавливал бы систему без сервера, добавленного соседом.
Почему не вносится сейчас: `collaboration.md` материал даёт, обязательная страница молчит — но лечится это не строкой в контракте, а механизмом: `kb_due.py` уже считает незапушенное, ему не хватает запуска **на выходе** из сессии, чтобы ловить автора, а не следующего. Это правка скрипта, и она ждёт окна.
Подтвердит: второй проект с двумя пишущими, где канон разошёлся из-за незапушенного. Опровергнет: запуск на выходе снимает случай без единой строки в контракте.

**`QUESTIONS.md` утверждает про настоящее, но исключением не объявлен.**
Предложил: тот же проект.
Аргумент: колонка «верный ответ» — утверждение о текущем состоянии, `valid_until` по контракту обязателен, файл его не несёт, проверка молчит. Протухает эталон, по которому проверяют правдивость базы: прогон пришлось делать с запретом смотреть в колонку ответов.
Почему не вносится сейчас: два разных выхода — объявить вторым исключением или не хранить в наборе устаревающих ответов, храня признак и источник. Второе ближе к духу (эталон, дублирующий канон, — снова правило 1), но это перестройка шаблона, купленная одним наблюдением.
Подтвердит: второй проект с протухшим эталоном. Опровергнет: проекты хранят в колонке признак, а не значение, и вопрос не возникает.

**Адресат производимого наружу фиксируется до структуры, а не после первой версии.**
Предложил: отчёт кроссплатформенной сессии по финансовой модели для внешнего адресата (одно наблюдение).
Аргумент: назначение документа было известно заранее, но первая версия строилась под автора — технический реестр источников, столбец статуса сверки, семь коммерческих потоков там, где продуктовых линий две. Владельцу пришлось отдельно напомнить, кто читатель, и модель пересобирали. Структура внешнего артефакта выводится из адресата, а не сокращается до него правкой.
Почему не вносится сейчас: это **добавляет шаг** ко всякой сборке исходящего, а куплено одним наблюдением. Ближайшая существующая конструкция — «выпуск наружу — политика, а не метка» (`authority.md` §6) — уже про исходящее, и не проверено, что её не хватает.
Подтвердит: второй случай в другом проекте, где артефакт наружу пересобирали из-за неучтённого адресата; либо случай, когда документ ушёл наружу неподходящим и это заметил получатель.
Опровергнет: проекты и так спрашивают адресата в момент постановки задачи, и отдельный шаг оказывается ритуалом.

**Класс состояния «незавершённое решение» и граница полномочий — модуль в справочник.**
Предложил: тот же отчёт (одно наблюдение).
Аргумент: контракт требует, чтобы вход отвечал на «что дальше», и это работает для стадий. Но согласованное решение живёт не стадиями: количества были согласованы, а лимитные цены, срок действия решения, налоговый статус и — главное — **граница между «согласовано аналитически» и «разрешено создать инструкцию»** остались только в чате. После перезагрузки часть состояния пришлось восстанавливать из диалога. Два противоположных риска сразу: переспросить уже согласованное и **принять аналитическое согласие за разрешение действовать**. Второй — того же семейства, что «уверенно и неверно», против которого написан весь стандарт.
Почему не вносится сейчас: предлагаемая форма (реестр со статусами `идея → исследовано → согласовано аналитически → параметры требуют проверки → инструкция создана → владелец отправил`) — это новый обязательный артефакт, а у входа уже есть раздел «ЧЕГО ЖДЁМ», и не проверено, что его не хватает. Сам отчёт честно оговаривает: «не доказывает, что отдельный реестр лучше небольшого раздела текущего входа».
Подтвердит: повтор в другом проекте, либо случай, когда раздел входа пробовали и он не удержал градацию полномочий.
Опровергнет: расширенного раздела во входе оказалось достаточно; либо реестр завели и он не обновляется — то есть стал второй базой решений рядом с чатом.

**Парная проверка перед сборкой исходящего — в контракт. ГЕЙТ ИНВЕРТИРОВАН.**
Это правило **необратимого яруса**, поэтому оно не ждёт подтверждения: эксперимент, сигналом успеха которого является утечка, ставить нельзя. Подтверждающее наблюдение к тому же уже есть — базу однажды пришлось чистить постфактум после отправки материалов наружу.
**Включается на первом же разборе по умолчанию**, если к тому моменту не появилось опровержения.
Опровергнет: проверка из справочника применялась по надобности и ни разу не потребовала напоминания — то есть контрактная строка была бы лишней.

**`data_as_of` для производных — в контракт.**
Аргумент: `updated` у производного всегда «сегодня»; без даты среза свежесть данных невидима.
Подтвердит: ответ по производному файлу оказался построен на старых данных.
Опровергнет: генераторы и так проставляют, правило избыточно.

**Утверждение об отсутствии требует доказательства наравне с утверждением о наличии — в контракт.**

Первое предложение, пришедшее **из эксплуатации**, а не из ревью. Медицинский архив, ~600 файлов: за одну сессию дважды объявлен открытым вопрос, закрытый годы назад и лежащий в базе. Между случаями в канал правок было записано правило «прежде чем писать „вопрос открыт“ — искать в базе»; правило не сработало через час.

Формулировка предложения: «этого нет», «вопрос открыт», «не сделано», «след обрывается» — утверждения о состоянии **базы**, а не мира; перед тем как их произнести, обязателен поиск по базе. Плюс симметричное поле к `verify` — назовём условно `searched` — для отрицательных утверждений.

*Почему не вносится сейчас, хотя аргумент сильный.* Под узкое определение дефекта не подпадает: контракт себе не противоречит и не ссылается на отсутствующую часть механизма. Это содержательное добавление, а таких за сессию разработки набралось бы двадцать — заморозка существует ровно для того, чтобы сила аргумента не решала. Сам отчёт это признаёт: «доказывает, что проблема настоящая, но не что предложенное решение работает».

*Две поправки к конструкции, которые надо учесть, если правило пойдёт в контракт.*

**Первая: поле `searched` рискует стать театром.** Написать «искал: везде» стоит ноль и не доказывает ничего. Это тот же провал, что у отметки синхронизации, которую ставит человек, — стандарт уже записал: **честные метаданные, которые ставит человек, не являются механизмом.** Значит принимать стоит только ту форму, где поиск **выполняется**, а не декларируется: строка запроса и его результат, а не самоотчёт о добросовестности.

**Вторая: возможно, это не пятое правило, а расширение второго.** Правило 2 гласит «не утверждать поведение внешней системы из памяти, сверься». Предлагаемое: «не утверждать отсутствие в базе, не сверившись с базой». Это одна и та же конструкция, применённая к другому объекту: там внешний мир, здесь сама база. Контракт из четырёх правил дешевле контракта из пяти, и если формулировка сводится к обобщению правила 2, вносить надо обобщение, а не новый пункт.

Подтвердит: в журналах за период набралось несколько записей класса «ложная тревога» (`measurement.md`), и порядок разбора из `operations.md` их не снял.
Опровергнет: порядка «темы → поиск по базе → выводы» оказалось достаточно, класс записей пуст или единичен. Тогда справочника хватило и контракт трогать незачем.

*Проверка на необратимый ярус — не проходит.* Цепочка рвётся на третьем звене: ошибка после выполнения устранима полностью — владелец отвечает «это уже есть», заметка правится. Цена — его время и доверие, а не невозвратный ущерб. Значит перевёрнутый гейт не применяется, действует обычный.

*Владелец предлагал разморозить контракт под это наблюдение (2026-08-02). Отклонено:* разрешение владельца — не факт эксплуатации, а снятие ограничения ровно в тот момент, когда ограничение работает. Заморозка защищает не от чужого давления, а от автора в минуту, когда аргумент хорош. Записано здесь, чтобы при следующем таком предложении было видно, что этот ход уже делался.

**Два черновика, чтобы разбор был выбором, а не сочинением.**

*Вариант А — обобщение правила 2, контракт остаётся из четырёх пунктов:*

> **2. Не утверждать состояние по памяти — ни чужой системы, ни собственной базы.** Про внешнюю систему сверься с ней; про базу — поищи в базе. «Этого нет», «вопрос открыт», «не сделано» — утверждения о базе, а не о мире, и требуют поиска так же, как утверждение о совершённом действии требует доказательства. Сверить нельзя — скажи «надо проверить», а не угадывай.

*Вариант Б — пятое правило, если А окажется слишком плотным:*

> **5. Утверждение об отсутствии требует поиска, как утверждение о наличии требует доказательства.** Поиск выполняется, а не декларируется: в записи остаётся запрос и его результат.

Оба варианта намеренно не вводят поля `searched` как самоотчёта: в контракт годится только форма, где остаётся след выполненного запроса. Поле, заполняемое от добросовестности, стандарт уже классифицировал как не-механизм.

**Триггер разбора:** три записи класса «ложная тревога» в журнале любого проекта, либо первый плановый разбор журналов — что наступит раньше.

---

---

# Обвинения девятого захода критики — не закрыты

Заход был инвертированным: «найди причины НЕ внедрять». Четыре его находки проверены и починены (выпуск 3.23). Ниже — то, что **не чинится правкой** и вынесено в разбор с владельцем. Записано здесь, а не в очередь, потому что очередь про предложения, а это диагнозы.

**Заморозка работает как громоотвод.** Страница формально не тронута, а за то же время справочник вырос вдвое и появилось 1100 строк Python. Обязательства мигрировали в незамороженные слои: шаблон правил велит запускать две команды при входе — контракт этого не требует; раздел «ПОРА» требует журнала, git и `kb_standard_version` — ни одного из них в контракте нет, а журнал в нём прямо назван «сознательно не вошедшим». **Действующий контракт — это вывод `kb_due.py`, и он растёт без гейта.** Возражение сильное и пока не опровергнуто.

**Темп выпусков — признак той самой болезни.** Пятнадцать выпусков за один день, шестнадцать минут между двумя, и запись в таблице «сделано вопреки собственной рекомендации остановиться» через тринадцать минут после самой рекомендации. Диагноз, который стандарт поставил себе в самом начале — «каждое возражение превращается в новый механизм за часы» — не вылечен, а переехал из контракта в скрипты и ускорился. Признаки, которые стоит считать: сколько выпусков переночевало непринятыми, сколько разборов кончились «ничего не меняем», батчатся ли правки.

**Первичных фактов эксплуатации нет.** Все «наблюдения с проектов» существуют только как пересказы здесь и в `rationale.md`, написанные тем же, кто принимает решения. Отличить факт эксплуатации от аргумента, рассказанного как случай, снаружи нельзя — а это единственная функция гейта. По собственной логике стандарта («проверка, которая не выполнилась, неотличима от пройденной») гейт без сохранённого сырья неотличим от церемонии.

**У кода, на котором держится проверяемость, нет тестов.** Тысяча строк с задокументированной историей: три выпуска подряд чинили ложь собственных скриптов, четвёртую нашёл внешний критик. Каждая починка добавляла эвристик, то есть расширяла поверхность следующей лжи. Стандарт с приёмочной метрикой «не соврать уверенно» поставляет инструменты, соврашие четырежды, без сетки, которая не даст соврать в пятый.

**Независимая переменная не зафиксирована.** Условие снятия заморозки — три проекта по месяцу с журналами. Но шаблоны и скрипты, через которые проекты соприкасаются со стандартом, менялись пятнадцать раз внутри окна наблюдения, и журналы наполняются записями о ложных тревогах самих инструментов. Разбор через месяц измерит текучку инструментария, а не работу четырёх правил.

**Что с этим делать — решает владелец, не сессия.** Варианты, названные критиком: выпускать батчем раз в период с обязательной записью отклонённого; вынести из обязательной страницы всё, что не правило; сократить аппарат `maintainer.md` до трёх строк; завести тесты скриптам. Ни один не вносится по силе аргумента — ровно по правилу, которое здесь же записано.

---

# Известные пробелы

Записаны здесь, чтобы не выдавать отсутствие механизма за отсутствие проблемы.

**Расхождение двух источников не обнаруживается машинно.** Первое правило контракта запрещает вести один класс состояния из двух мест, но проверить это скриптом в общем виде нельзя: «та же величина в другом файле» распознаётся только по смыслу. Сейчас ловится тремя способами, все ручные: диагноз без изменений (`adopt-existing.md` 1а), контрольные вопросы, свежий читатель со стороны. Последний оказался самым результативным — он не знает, что «имелось в виду».

**Фиксация перед обрывом контекста держится на внимании сессии.** Правило «близко к сжатию — предложи зафиксировать незаписанное» проваливает тест цены ровно так же, как провалилось правило о поиске перед выводом: в момент, когда контекст на исходе, сессия занята задачей. Автоматизировать нечем — сессия не знает своего остатка надёжно. Пока смягчается тем, что запись идёт по ходу, а не в конце.

**Никто не проверяет, что скилл вообще применяется.** Файлы контракта могут лежать, а сессии их игнорировать. `kb_due.py` заметит протухший вход, но не заметит, что база просто не используется.

**Восстановление из бэкапа ни разу не проверялось.** Ветка-снимок и тег заводятся, `git bundle` делается, но полный цикл «потерять и восстановить» не прогонялся ни на одном проекте. Резервная копия, которую не восстанавливали, — предположение, а не копия.

**Инструменты знают ограниченный набор мест, где проект может держать вход, журнал и вопросы.** Поиск в `kb_paths.py` перечислен явно: объявление в правилах или в шапке входа, известные имена на два уровня вглубь, раздел с известным заголовком в правилах, `KB.md`, входе. Проект, устроенный иначе, снова не будет найден. Класс от этого не исчез — он только перестал быть тихим: промах теперь печатается находкой, а отчёт называет объём проверенного. **Расширение поиска чинит случай, различение исходов чинит класс** — и второе стоит делать первым.

**Шестой случай механизма, уничтожающего условие собственного применения.** Способность «сравню редакцию проекта с установленной» потребляла поле, которое ничто не велело создавать: шаг, на котором данные появляются, отсутствовал. Сигнала не возникало — сравнивать было нечего, и молчание читалось как согласие. Отличие от пяти предыдущих: здесь механизм не умирал от собственного успеха, а **не рождался**, и обнаружить это можно было только снаружи. Общее у всех шести — отсутствие сигнала выглядит как норма.

---

# Внешняя экспертиза

Что можно критиковать и как — три позиции, а не две:

| Действие с контрактом | Можно? | Почему |
|---|---|---|
| **опровергнуть конструкцию** | да, свободно, и это самое ценное | опровержение само по себе не меняет текст |
| **указать дефект** — внутреннее противоречие или неполный механизм | **чинится сразу** | это проверяемый факт о тексте, а не мнение |
| **предложить изменить объём** — и добавить, и выбросить | через этот список | сокращение по аргументу — та же правка по аргументу |

Исключение: правило **необратимого яруса** проходит гейт в обратную сторону — включается и ждёт опровержения. Ставить эксперимент, сигналом успеха которого является утечка или подписанный не тот документ, недопустимо.

## Промпт для внешнего эксперта

Приложить: выгрузку скилла одним файлом (или сам скилл, если модель умеет читать архив).

```
Ты выступаешь внешним экспертом. Нужна не вежливость, а польза.

КОНТЕКСТ

Я веду четыре разнородных проекта, работаю с каждым через AI-ассистента, у
которого между сессиями не сохраняется контекст. Проекты разные по природе:
юридическое дело со сроками; операционный проект вокруг живой ERP, где правда
живёт в проде; семейный медицинский архив, где ~90% файлов генерируются
скриптами; долгоживущий архив-досье, написанный руками.

Во вложении система, которую я собираюсь применять ко всем четырём и
разворачивать в новых. Она состоит из двух неравных частей:

1. КОНТРАКТ — одна страница. Обязательный минимум, одинаковый везде.
   Он ЗАМОРОЖЕН на время обкатки: добавления вносятся только по фактам
   эксплуатации, а не по аргументам. Причина: предыдущие версии росли быстрее, чем
   проверялись, и в какой-то момент любая критика превращалась в новую
   версию вместо проверки. Расти обязательствам опасно.

2. СПРАВОЧНИК — всё остальное. Ничего не обязывает, берётся по надобности.
   Вот его улучшать можно и нужно: плохой раздел справочника просто не
   откроют, а плохое обязательство соблюдают все.

ЧТО ОТ ТЕБЯ НУЖНО

ЧТО МОЖНО КРИТИКОВАТЬ И КАК — ВАЖНО

Асимметрия здесь не «контракт священен». Она в другом: **опровергать можно
свободно, а менять объём — и вверх, и вниз — только по фактам эксплуатации.**

- **Опровергать философию контракта — можно и нужно.** Если сама идея минимума
  неверна, если правило вредно, если конструкция «маленькое ядро + свободный
  справочник» ошибочна — бей прямо и в полную силу. Это самое ценное, что ты
  можешь сделать.
- **Предлагать выбросить из контракта — можно, но через отложенный список,**
  наравне с добавлениями. Сокращение по аргументу — та же правка по аргументу.
  Правило, которое работает, в журнале эксплуатации неотличимо от ненужного:
  пойманная ошибка не наступает, писать нечего. Односторонний журнал сойдётся
  к пустой странице.
- **Предлагать добавить в контракт — только с условием.** Вынеси отдельным
  пунктом и обязательно объясни: КАКОЕ НАБЛЮДЕНИЕ ЭКСПЛУАТАЦИИ подтвердило бы
  твою правоту. Такие пункты я откладываю и сверяю с тем, что реально сломается.
  Причина не в упрямстве: именно цикл «возражение → новый механизм» и раздул
  предыдущую версию до 1900 строк за один день.

Справочник критикуй без ограничений, но учти: у него есть потолок (десять
модулей, двенадцать разделов) и правило вытеснения — новый раздел входит
вместо старого, а не рядом. Предложение «добавить раздел» должно называть,
что оно сокращает.

## 1. Философия контракта: она верна?
Сама конструкция «маленький замороженный контракт + свободно растущий
справочник» — работающая или самообман? Если самообман, объясни механизм,
которым она развалится.

## 2. Контракт: что лишнее
Бей без пощады: сокращение принимается сразу.

## 3. Философия: чего не хватает на уровне понятий
Не правил, а понятий. Какими категориями система не оперирует, хотя они
определяют результат? Опирайся на реальные практики работы с знанием и
данными, а не на пересказ вложенного.

## 4. Справочник: что неверно
По разделам. Где написана ерунда, где рекомендация вредна, где обоснование
не следует из источника, где паттерн описан хуже, чем он есть в известной
практике.

## 5. Справочник: чего в нём нет
Что стоило бы туда добавить и почему. Формулируй как раздел, который можно
взять и положить.

## 6. Слабейшее место всей затеи
Одно. Самое.

## 7. Что бы ты выбросил
Из справочника. С обоснованием, почему это не окупает даже нулевой
обязательности.

ПРАВИЛА ОТВЕТА
- Не пересказывай вложенное.
- Отличай «я знаю» от «я предполагаю» — помечай явно.
- Конкретика важнее полноты: один раздел, написанный так, что его можно
  вставить, полезнее десяти замечаний.
```

---


## Что делать с ответом

Дефекты — чинить сразу. Предложения по объёму — сюда, в очередь, с наблюдениями в обе стороны. Предложения по справочнику — вносить, если они называют, что вытесняют.

Несколько экспертиз от разных моделей полезнее одной, но **совпадение выводов доказывает общность моделей, а не общность правила.** Расхождения информативнее совпадений.

---

## Правило разбора

На разборе по каждому пункту смотрится **журнал эксплуатации, а не сила аргумента**.

**Период считается испытательным только тогда, когда хотя бы раз наступило условие применения правила.** Это главная поправка ко всему механизму. Иначе редкие правила с событийным триггером — согласование реорганизации, проверка перед отправкой наружу, восстановление после паузы — удаляются не потому, что непроверяемы, а потому что триггер не возник за календарную неделю.

Поэтому счёт ведётся **в наступлениях, а не в неделях**:

- у пункта есть триггер → «три периода» означают **три наступления триггера**, сколько бы недель они ни заняли;
- триггер за период не наступал → период для этого пункта **не засчитывается**, пункт остаётся в очереди без штрафа;
- у пункта нет триггера, он про повседневное → работает календарь, три периода.

**Превентивное правило оценивается не по отсутствию ущерба.** Отсутствие утечки не доказывает ни что правило сработало, ни что риск не возникал, ни что правило вообще применялось. Считается другое: сколько раз возникала точка применения · сколько раз проверка фактически выполнялась · что она изменила · сколько создала ложных остановок · можно ли перенести её в шаблон или в разрешение инструмента вместо контрактной строки.

**Знаменатель важнее ярких случаев.** По полям это значит считать не «поймал расхождение», а долю: сколько встретилось внешних действий, у скольких поле назначилось однозначно на уровне файла, у скольких потребовало структуры, к скольким неприменимо. Такая дробь наблюдаема за неделю даже без единого инцидента — в отличие от «поймал», который требует редкой цепочки совпадений.

