# История выпусков — машинная

Этот файл читает `scripts/kb_apply.py`: он достаёт строки между редакцией проекта
и установленной и показывает, чего они касаются **в этой базе**. Так вопрос
«что изменилось» стоит около полутора тысяч токенов вместо тринадцати.

**Рабочей сессии читать не нужно, и открывать целиком — дорого:** файл весит
примерно 13 000 токенов, это две с половиной обязательных страницы контракта.
Нужна одна строка — возьми её скриптом:

    python3 scripts/kb_apply.py <корень базы>

Вынесен из `rationale.md` в 4.10 по замеру: он составлял 80% того файла, и любая
сессия, открывшая «почему правила такие», платила за всю историю заодно.

## 0. Что изменилось в v2 и по каким возражениям

v1 была проверена на трёх реальных разнородных проектах. Все три вынесли «внедрять частично». Изменения ниже — следствия конкретных фактов из репозиториев, а не пересмотра вкусов.

| Что было в v1 | Что стало | Почему |
|---|---|---|
Роли и пути объявляются строками в файле правил проекта — их читает `kb_paths.py`. Отдельного конфига нет: `.kbconfig.yml` удалён в 4.0, потому что его не читал ни один скрипт, а объявленные в нём пути и лимиты были вторым источником правды рядом с файлом правил.
| Десятичная нумерация в ядре | **Опциональный модуль с запретом там, где имена читает код** | Отвергнута всеми тремя. За сто с лишним коммитов переименований не случилось ни разу; заявленная польза покупает то, что уже есть |
| Лимиты в строках | **Лимиты в килобайтах** | Файл на 103 строки прошёл бы проверку 150 строк и весил при этом 24 КБ — в шесть раз больше собственного бюджета. Мера должна совпадать с обоснованием (расход контекста) |
| Один класс файлов | **Три профиля: authored / derived / mirror** | На проекте с генератором под правила v1 подпадало 8% файлов. Для остальных каждое правило либо шумело, либо врало |
| Протухание = возраст файла | **`review_by` важнее возраста; `verified_at` для внешних систем** | Механизмы v1 ловят протухание от нашего бездействия и не ловят от чужого действия. Файл-конспект прода никто не трогал — и именно поэтому он мёртв |
| `NEXT 3` жёстко | **`NEXT` по фронту с потолком на число фронтов; не вести руками, если генерируется** | Три пункта на четыре параллельных дела означают «забудь про просроченный срок, потому что он четвёртый» |
| Лимит 500 строк на всякий `.md` | **Лимит только на authored; классы `spec` и `prompt` выведены** | Спецификация процедуры и внешний промпт монолитны; разрезание спеки гарантирует, что кто-то прогонит четыре части из пяти |
| Линтер как обязательный шаг | **Сначала настройка, потом обязательность** | На реальном репозитории v1 давал сотни ложных срабатываний. Линтер, выключенный на второй день, хуже отсутствующего — на нём висит всё обещание проверяемости |
| Отдельная папка выходов | **Роль по потребности** | Производимое наружу уже живёт привязанным к делу или поставке; отдельная папка раздваивает |
| «Пишет один агент» | **Владение по путям в ядре** | Неверно в обоих проверенных проектах: в один репозиторий пишут разные чаты |
| Зачёркивание запрещено | **Запрещено в списках задач; для отозванных выводов предпочтительно** | `superseded`-файл разрешено не читать и он уезжает в архив; зачёркнутая строка стоит на пути каждой сессии — и поэтому ошибка не возвращается |
| Стартовый промпт — одноразовый артефакт | **Указатель — постоянный, в файле правил; дельта — одноразовая** | v1 склеила два жанра и выписала правило по худшему. Указатель состояния не содержит и живёт месяцами |
| «Рабочую конвенцию не ломай» | **Критерий: уступает правило, чьё нарушение не обнаруживается автоматически** | Без критерия это не протокол, а разрешение на бардак: решение принимает сессия, и принимает молча |
| Провенанс — свободный текст | **Для внешних действий обязателен идентификатор, иначе `TBD`** | Утверждение «подано» проверяемо позже только через чужую систему |
| — | **Фиксация по границе контекста** | Все правила говорили, как хранить; ни одно — когда фиксировать |
| — | **Git-контракт в ядре** | База живёт в git; коммит, push и точка возврата — часть обязательств, а не фон |
| Чувствительность = приватный репозиторий | **`sensitivity` как атрибут записи + решения о доступе отдельным классом** | Приватность защищает от посторонних, но не от выноса фрагмента наружу |

### Правки v2.1 — из практики, а не из ревью

Шесть находок медицинского проекта, каждая оплачена реальной ошибкой. Общий их смысл: **все прежние правила защищают знание внутри одного контура, и ни одно — от того, что контуров два.** Расхождение контуров переведено из побочного риска в шестое обязательство ядра.

| Что добавлено | Чем оплачено |
|---|---|
| **Аудит проводится только по источнику**; без сверки отметки сессия не вправе судить о состоянии базы | Ревью по зеркалу выдало ложную находку о противоречии в правилах — в источнике формулировку переписали двумя днями раньше. Находка выглядела настоящей и потребовала работы |
| **Отметка синхронизации машинная и обязательная**, её пишет скрипт, а не человек | Расхождение 38 коммитов и 309 файлов; зеркало отвечало «имя неизвестно, состояний не записано» по человеку со 153 записями анализов за 13 лет. Ни один файл не сообщал |
| **Бюджет яруса — явное число в конфиге** | Потолок 2 МБ при 1.3 МБ одних структурированных профилей: состав яруса выбирается под потолок, а не по важности |
| **Манифест зеркала с разделом «чего здесь нет»** | Без него отсутствие файла читается как «данных нет», а не «данные в источнике». Для медицинского архива это разные ответы |
| **Оперативные представления не зеркалятся вообще** — подкласс `derived` | «Что сегодня», доска состояния протухают за дни; копия не даёт ничего и гарантированно врёт |
| **Реестр отмен с адресами**, а не только `status: superseded` | Отменяемая формулировка жила в шести местах четырёх файлов; пометка одного файла оставила пять конкурирующих источников |
| **Шаг цикла с исполнителем**: «что осталось человеку и почему» | У моста нет сети, `push` остался человеку — цикл считался замкнутым, будучи открытым: коммиты есть, на удалённом нет |

### Правки v2.2 — от сессий, которые стандартом пользуются

Два разбора «изнутри»: не «подходит ли стандарт проекту», а «каково им пользоваться». Обе сессии независимо указали на один перекос: **стандарт написан про хранилище и почти ничего не говорит про путь записи и про сверку с внешним миром.** Объяснение точное — сессия с полным контекстом не может оценить навигацию, потому что для неё всё уже найдено; правила структуры видны автору, правила записи видны только тому, кто терял контекст.

| Что добавлено | Чем оплачено |
|---|---|
| **Запись против утверждения.** `updated` — факт об авторе, читателю нужен `valid_until`; истечение теперь ERROR, `type: snapshot` требует срок обязательно | Файл-снимок с честным `updated` был мёртв три месяца при безупречных метаданных, линтер бы пропустил. Дефект модели, а не реализации |
| **Раздел «когда писать»** — три триггера, равный по весу «куда писать» | За месяц ни один факт не потерялся из-за структуры; терялись при сжатии контекста. Единственное сработавшее правило было операционным |
| **Критерий проектирования: соблюдать должно быть дешевле, чем нарушать** | Панель выросла в летопись не по глупости: дописать строку — 5 секунд, разложить правильно — 3 минуты. Правило, проваливающее тест, надо автоматизировать или выбросить |
| **`verify` — ключ обратной проверки** рядом с `source` | «Откуда знаю» не помогает через полгода. Нужно «чем подтвержу заново в чужой системе»: номер подтверждения, номер анализа, sha, message-id |
| **Режим `reconcile`** — сверка с внешним миром, отдельно от аудита | Дыра нашлась распаковкой отправленного документа, а не из базы: линтер markdown такой класс не видит принципиально |
| **`no_export` и проверка на выходе** | Утечка идёт не из хранилища, а при сборке исходящего артефакта. Базу уже чистили постфактум |
| **Раздел UNKNOWN** в «где мы» | Незаписанное незнание опаснее плохо помеченного знания: на месте пустоты сессия сочинит правдоподобное |
| **Тип `attempt` + `stopped_at`** | Брошенная попытка не решение, не факт и не история — проваливается между классами и предлагается снова |
| **`CORRECTIONS.md`, append-only, без спроса** | Пока сообщить об ошибке дороже, чем продолжить задачу, побеждает «продолжить». По цене, не по злому умыслу |
| **Каналы: чем читать и чем писать** | Сканы и PDF видны только через перенос и чтение глазами; большие операции — только через оболочку. Файл, «скачанный» в песочницу, на диске владельца не появляется |
| **Злой тест как главная метрика** | Тест «отвечает с трёх файлов» проверяет полноту. Нужна способность **не соврать уверенно**: вопрос, на который база отвечает противоречиво |
| **Вторая ось профилей — тип центрального объекта** (обязательство со сроком / наблюдение во времени / воспроизводимое состояние) | Один каркас верен в середине и неверен на краях. Ось «кто пишет» осталась основной, предметная закрыта модулями |

### Правки v2.3 — с полей одной операции

Разбор одной пересинхронизации зеркала. Часть находок подтверждена наступившей ошибкой, часть выведена из устройства проекта и помечена как непроверенная — вес разный, и смешивать их нельзя.

**Подтверждено фактами:**

| Что добавлено | Чем оплачено |
|---|---|
| **Асимметрия удаления: в зеркале устаревшее удаляется, а не помечается** | Устаревшая копия хуже отсутствия: отсутствие честно отсылает к источнику, копия врёт с видом факта, и `status` не спасает — читатель открывает файл, а не метаданные. Из зеркала удалено 95 файлов, не потерян ни один |
| **Второй критерий проектирования: процедура должна помещаться в один сеанс** | Наведение порядка в зеркале — около 130 операций. Такое делают один раз и перестают. Ужимать надо состав яруса, а не автоматизировать процесс |
| **Отмена — связь один-ко-многим**, `superseded_by` её не выражает | Одна формулировка в шести местах четырёх файлов, включая строку внутри исполняемого скрипта |
| **Правку производного делают в генераторе** — подтверждено буквально | Устаревшая строка сидела в коде, печатающем её в каждый вывод; правка вывода исчезла бы при следующем запуске |

**Выведено, но не проверено наступившей ошибкой:**

| Что добавлено | Рассуждение |
|---|---|
| **Объявленный уровень соответствия + `kb_standard_version`** | Стандарт бинарен, применяется всегда частично. Без объявления следующая сессия не отличит осознанный отказ от недоделанной миграции и начнёт «доводить». Плюс стандарт сам протухает — без версии повторит болезнь, от которой лечит |
| **Удаление персональных данных — исключение из вытеснения** | Удаление по требованию обязательно и пометкой не заменяется; но нужен реестр удалённого, иначе документ вернётся из почты и будет обработан заново |
| **Интервалы валидности `valid_from` / `valid_to`** | Цепочка вытеснения даёт «последнее верное», а нужен ответ на «что было верно в марте». Большая часть знания не заменяется, а имеет срок |
| **Фиксированные вопросы вместо выдуманной типовой задачи** | «Типовая задача» каждый раз новая, результаты несравнимы. Один и тот же набор вопросов делает деградацию измеримой |
| **Бюджет холодного старта в килобайтах** | «Три файла» — плохая единица: могут стоить 5 КБ, а могут 200. Лимит на карту становится следствием бюджета, а не магическим числом |
| **Модуль `entities` — реестр сущностей и алиасов** | Один человек в четырёх написаниях; одно юрлицо в пяти у пяти поставщиков. Нумерация файлов это не решает: она про адрес файла, а не про тождество объекта |
| **Языковая политика** | Язык ключей, содержимого, выходов; нормализация терминов с сохранением исходного написания в провенансе — иначе показатель из двух лабораторий не сойдётся в один ряд |
| **Модуль `access_log`** — с честной оговоркой | «Что мертво» определяется по дате правки, а это неверная прокси: справочник не правится годами и открывается еженедельно. Но пока запись не автоматизирована хуком, модуль проваливает первый критерий проектирования |

### Журнал правок во время заморозки — честная классификация

Внешняя проверка показала, что часть правок прошла через «дефектную дверь» шире, чем дверь позволяет. Записано как есть, без попыток задним числом переквалифицировать:

| Правка | Класс по факту |
|---|---|
| Вход: свободный канон против обязательного файла | **дефект** — внутреннее противоречие текста |
| Убрать метку «наружу не выносится» | **дефект** — неполный механизм: метка без парной проверки, fail-open |
| Ротация злого вопроса | **дефект** — механизм уничтожал условие собственного применения |
| `verify`: «без идентификатора это TBD» → «достаточное доказательство» | **смысловая правка, не дефект.** Текст себе не противоречил, он задавал спорное правило доказательства. Прошла через дефектную дверь; оставлена, потому что новая формулировка менее ложна, но классифицируется как нарушение заморозки |
| Отметка свежести и потолок 8 КБ у входа | **протащено вместе с ремонтом.** Само противоречие требовало только одного: снять обязательность конкретного файла. Остальное — архитектурное решение |

Смысл этой таблицы — не самобичевание, а защита двери. Если задним числом называть смысловые правки дефектами, категория расширяется по цепочке «слишком узкая формулировка → неполный механизм → немедленная правка», и заморозка перестаёт что-либо значить.

### Выпуски скилла

Номер версии стоит в шапке `SKILL.md` и повторяется полем `kb_standard_version` в правилах проекта. **Номер обязан меняться при любой правке содержимого пакета** — иначе владелец, у которого стоит скилл, не отличит одну сборку от другой, а проект, записавший версию, зафиксирует ложь.

Различие номеров: правка справочника меняет вторую цифру, правка контракта — первую. Контракт заморожен, поэтому первая цифра меняться не должна вовсе, пока заморозка не снята.

| Версия | Что изменилось | Класс |
|---|---|---|
| 3.0 | контракт на одной странице + справочник; заморозка контракта | базовая сборка |
| 3.1 | `start-new.md`: случай «облачная сессия уже набрала данных» — сырьё в репозиторий до всякой структуры · `start-new.md` и `adopt-existing.md`: длинные тексты для копирования заменены одной фразой · `SKILL.md`: строка меню и оглавление справочника | справочник; в контракте затронута только навигация |
| 3.2 | `kb_init.py`: `--areas` больше не заводит `INDEX.md` вне контракта — план областей дописывается в карту, если она есть, иначе в файл правил, и попадает в отчёт о созданном · шаблон `QUESTIONS.md`: убрано противоречие контракту («старый вопрос остаётся в наборе» → уходит в архив набора), добавлен раздел архива | справочник и скрипты |
| 3.3 | `collaboration.md` §1.1 — подключение внешнего ИИ другой платформы: почему скилл ему не даётся, три способа держать один свод правил без копии, порядок «закоммитить всё → только чтение → вердикт словами → запись в свою ветку», что развести до первого чужого коммита, чего ждать от первого прогона · строка меню в `SKILL.md` | справочник |
| 3.4 | `SKILL.md`: раздел «Если ты видишь это впервые» — задача, что делает, чем отличается, что честно сказать сразу, как начать. Для того, кому скилл прислали, а объяснения к нему не приложили | **правка страницы контракта.** Классифицируется честно: это не дефект и не правка правил, а пояснительная рамка перед ними. Ни одно обязательство не добавлено, не убрано и не переформулировано; заморозка распространяется на правила, и этот случай её не нарушает, но и не является исключением из неё — он вне её предмета |
| 3.5 | подготовка к публикации: убраны последние привязки к проектам автора (пример `mirrors:` c конкретным id внешней системы → плейсхолдер), `author` → ник. Содержание правил не менялось | справочник и шапка |
| 3.6 | **первая правка по факту эксплуатации, а не по ревью.** `operations.md` — «Разбор нового источника: сверка до вывода, а не после»: обрыв сюжета внутри источника означает «не было в этом канале», а не «не было»; порядок «темы → поиск по базе → выводы». `measurement.md` — пятая величина и третий класс записи в журнале: ложная тревога, то есть случай, когда **сессия соврала о базе**, а не база о мире. `maintainer.md` — предложение о симметричном доказательстве для утверждений об отсутствии положено в очередь с двумя поправками к конструкции | справочник |
| 3.7 | `scripts/kb_lookup.py` — поиск по базе по темам с вариантами написания, запускается до формулировки вывода. Правило из 3.6 заменено на команду: правило, соблюдать которое дороже, чем нарушать, по критерию стандарта автоматизируется или выбрасывается. `measurement.md` — счёт ложных тревог понижен из механизма в сигнал фальсификации: владелец не измерительный прибор | справочник и скрипты |
| 3.8 | `00-kak-chitat.md` §0 — диагностика из пяти вопросов: что из справочника относится к этому проекту, определяется механически, а не на вкус. `assets/templates/defect-report.md` — жанр отчёта о дефектах скилла для того, кто ведёт стандарт. `start-new.md` — «сразу или дать поработать»: запись немедленно, структура по знанию владельца. `maintainer.md` — объявлено условие снятия заморозки и признак её вырождения в церемонию | справочник и шаблоны |
| 3.9 | `scripts/kb_due.py` — что просрочено, первым действием сессии: вход, ожидания, журнал, контрольные вопросы. Даты берутся из файлов контракта, новых обязательств не появляется. Заменяет памятку в голове владельца — по тому же правилу, по которому скрипт заменил правило о поиске. `operations.md` — команда как первый шаг ежедневного цикла | справочник и скрипты |
| 3.10 | `adopt-existing.md` часть 1а — диагноз без изменений: третий режим между «не трогать» и «перестроить», список проблем по цене и правило чинить по одной по ходу дела, а не кампанией. `defect-report.md` — куда отправлять отчёт, и правило, что сессия предлагает это сама, записав строку в журнал | справочник и шаблоны |
| 3.11 | из шаблона отчёта убрана почта автора: скилл публичный, адрес в нём стал бы публичным и собираемым спам-ботами. Канал один — issue в репозитории; непубличный материал идёт через владельца проекта | справочник |
| 3.12 | **ревизия против цели «не думать о хранении».** `scripts/kb_check.py` — целостность тремя проверками, которые не шумят (битые ссылки, истёкшие сроки, пустые `verify`); проверен на четырёх живых базах, нашёл настоящую поломку в документе, ушедшем внешнему эксперту. `scripts/kb_lint.py` **удалён**: две тысячи строк, ни разу не проверенных на живом репозитории, и собственная пометка «даст шум» — по правилу стандарта это TODO, а не инструмент. `kb_due.py` — добавлено состояние git: незакоммиченное и незапушенное. Шаблон правил проекта — обе команды в разделе «Вход в сессию», чтобы сессия запускала их сама, а не по просьбе. `measurement.md` — процедура разбора журнала. `maintainer.md` — раздел «Известные пробелы» | справочник и скрипты |
| 3.13 | `assets/templates/first-run.md` — записка владельцу после первой настройки, обязательный последний шаг обеих процедур: что появилось, что от него нужно, что сессия делает сама. Без неё человек получает пять файлов и молчание, а молчание читается как «теперь надо что-то соблюдать». `start-new.md` — исправлена подача развилки «сразу или потом»: она была описана как выбор владельца, хотя решается по признаку, видному сессии. Владельцу сообщается результат, а не вопрос | справочник и шаблоны |
| 3.14 | **сервисные работы срабатывают по объёму, а не только по времени.** `kb_due.py` — сколько изменилось за неделю (большой прирост требует проверки целостности) и рост базы против записанной базовой линии диагностики. `operations.md` — коммит по объёму несохранённого вместо «почувствуй, что контекст кончается»: порог в один содержательный блок, порядок «знание в файлы → вход → коммит → push», и прогон проверки целостности перед завершением большой сессии. Шаблон правил — строка базовой линии | справочник и скрипты |
| 3.15 | `collaboration.md` §3а — приём хендовера. Отдающая сторона была описана, принимающая нет: сессия, получившая аккуратный отчёт, применяет его целиком, потому что он выглядит как знание, а не как источник. Порядок: сверка с базой, разбор на четыре кучи, расхождения в канал правок, ответ отправителю. Шаблон хендовера — три блока для принимающей стороны: чем подтверждается, чего не проверял, что у вас возможно уже есть. **Сделано вопреки собственной рекомендации остановиться** (выпуск 3.14): достройка половины существующего механизма, а не новая конструкция | справочник и шаблоны |
| 3.16 | **два дефекта из эксплуатации, оба в скриптах.** Первый: разбор дат понимал только ISO, и проект с форматом `DD/MM/YYYY` получал систематическую ложную тревогу «контрольные вопросы ни разу не прогонялись» в день, когда прогон был записан. Формат дат контрактом не задан — обязателен инвариант, реализация за проектом; инструмент наказывал за буквальное следование контракту. Починено общим модулем `kb_dates.py`: три записи, порядок дня и месяца выводится по всей базе, при неразрешимой неоднозначности **скрипт молчит, а не утверждает «ни разу»**. Второй: потолок входа (~8 КБ, единственный численный лимит контракта) не проверялся ничем, и отступление могло выглядеть соблюдением, если проект записал лимит в строках. Добавлена четвёртая проверка в `kb_check.py` и измерение входа в процедуру присоединения | скрипты и справочник |
| 3.17 | подробность отчёта о дефектах стала политикой проекта, а не свойством шаблона. Обезличенный режим остаётся умолчанием, потому что скилл публичный и безопасное поведение должно быть тем, которое получают по умолчанию. Детальный включается строкой в правилах при трёх условиях сразу: приватные репозитории, владелец и получатель одно лицо, непубличный канал — тогда получатель может пойти и посмотреть сам, вместо того чтобы переспрашивать. Данные третьих лиц исключены в обоих режимах | шаблоны |
| 3.18 | **три дефекта из эксплуатации, все в моих инструментах, и все одного вида: инструмент судил не о базе, а о своей мерке.** (1) Потолок входа проверялся только для `NOW.md`/`STATUS.md` в корне; у проекта с входом в подпапке проверка молча пропускалась, и печаталось «чисто» при превышении в 1.8 раза. Тот самый fail-open, за который контракт изъял у себя четвёртое поле, — в собственном инструменте. (2) Журнал искался одним именем в корне, а проект вёл его разделом внутри другого документа: ложная тревога «журнала нет» о журнале с семью записями. (3) `kb_standard_version` был объявлен способностью и не имел шага, на котором данные появляются: поле никто не заполнял, и никто не замечал незаполненного. Починено общим модулем `kb_paths.py` (объявление в правилах или шапке входа, известные имена на два уровня вглубь, раздел с известным заголовком) — но **чинит класс не расширение поиска, а различение исходов**: промах теперь печатается находкой «ПОТОЛОК НЕ ПРОВЕРЕН», а отчёт всегда называет объём проверенного. Плюс: сравнение редакции проекта с установленным скиллом, номер требуется числом; `collaboration.md` §1.2 — доменный скилл поверх контракта ссылается, а не пересказывает; в процедуре присоединения последний шаг — прогон, подтверждающий, что инструменты видят объявленное  ⟦Д: запиши в правила проекта строку `kb_standard_version: <номер>` — без неё нельзя понять, по какой редакции живёт база⟧| скрипты, справочник и шаблоны |
| 3.19 | найдено при применении 3.18 к собственным проектам: объявление, обёрнутое разметкой — маркером списка, обратными кавычками, полужирным, — разбором не опознавалось, и проект с записанной редакцией получал «редакция не записана». Тот же класс, что чинили выпуском раньше: **оформление решало, видит ли инструмент факт**. Живые проекты пишут строку так, как удобно человеку, и это не отступление. Разбор объявлений допускает разметку вокруг ключа и значения. Плюс: **объявленное отсутствие — это ответ, а не молчание**. Проект, честно записавший «журнал не заводим, за месяц ни одной записи», получал тревогу «журнала нет» каждую сессию — контракт разрешает отказ, если он объявлен, а инструмент требовал его заново. Ложная тревога дешевле пропуска, но не бесплатна: после второй раздел «ПОРА» перестают читать | скрипты |
| 3.20 | **закрыты два наблюдения, пролежавшие открытыми с отчёта 3.15,** и один дефект в собственном шаблоне. (1) *Скилл — тоже внешняя система.* Правило «сверься, а не помни» предполагает, что система между наблюдением и сверкой не менялась; со скиллом это неверно — он обновляется молча и как раз в ответ на присланный отчёт. Сессия на проекте переписала три записи в базе на «причина неизвестна, не воспроизводится», сравнив наблюдение с уже починенным кодом. `kb_due.py` печатает установленную редакцию при каждом входе; `operations.md` — признак случая и порядок действий при выводе, опровергающем собственное недавнее наблюдение. (2) *Ключ к контрольным вопросам лежит внутри проверяемой базы:* субагент с чистым контекстом нашёл колонку «верный ответ» сам. Названо прямо, добавлены три варианта по возрастанию цены. (3) Шаблон отчёта содержал правило о данных третьих лиц **дважды**, разными словами — первое правило контракта, нарушенное внутри собственного шаблона; найдено при чтении шаблона снаружи. Плюс: шаблон запрещает копировать себя в базу проекта — копия расходится с оригиналом молча | справочник, шаблоны и скрипты |
| 3.21 | **первый отчёт с другой платформы** (Codex, инвестиционный проект) — семь наблюдений, и главное в нём не они, а то, что он вскрыл в самом порядке разбора. Починено сразу, потому что это ошибки, а не предложения: шаблон отчёта не спрашивал **редакцию скилла и среду** — и потому пришёл отчёт, который не к чему приложить (номер в правилах проекта говорит, что проект объявил, а не что установлено); `authority.md` §1а — **реестр отвечает только за свою область**: инвентарь локальных инструментов честно отвечал за локальные, вывод «коннектора нет» был сделан за его границей и оказался ложным при работающем коннекторе; §1б — **отчёт о проверке называет охват**, второй независимый случай того же класса, что дефект 3.18: автоматизация обещала два портфеля, прочитала один, оформила как общий. В `maintainer.md` разделено то, что раньше сливалось: **ошибка чинится сразу, в очередь идёт новая обязанность** — очередь перестаёт быть местом, куда прячут известную поломку; владелец может снять ожидание, но только модулем в справочник и с записанным условием отката; у отчёта появился признак «разобран до конца» — выпуск 3.16 взял два наблюдения из четырёх, и заметилось это случайно. В очередь с условиями: карточка предусловий перед необратимым внешним действием, класс состояния «незавершённое решение» с границей полномочий | справочник и шаблоны |
| 3.22 | **второй отчёт с Codex**, другой проект. Починено сразу: (1) `authority.md` §1а — **проверка должна быть того же рода, что и намерение**. Формулировка «минимальная безопасная проверка», написанная выпуском раньше, была неполна: собирались писать в таблицу, проверили тем, что файл виден и читается, несколько раз объявили доступ подтверждённым — а подключение имело права чтения без записи. Там же разведены три разные вещи, которые смешивают: права человека, права на объект, область полномочий подключения; и правило «после подтверждённого блокера — рабочий маршрут, а не вариации провалившегося». (2) §1б — **статус не шире доказанного**: сообщение по ходу несёт доказанно сделанное, найденный блокер или выполняемое сейчас; «доступ должен работать» — надежда в грамматике отчёта. Признак: можно ли приписать «— проверено тем-то». (3) `operations.md` — **результат, отданный ссылкой на временное пространство, не сохранён**, и проверяется факт сохранения, а не запрос на него: два случая подряд, включая отклонённую установку самого скилла. Плюс убраны упоминания линтера в цикле завершения блока и в git-контракте. **Слово «последние» здесь ложно**: поиск шёл с заглавной буквы, одиннадцать упоминаний со строчной остались, семь из них предписывающие. Исправлено в 3.23; строка оставлена как была — чейнджлог, переписывающий собственные ошибки, перестаёт быть свидетельством. В очередь: адресат исходящего фиксируется до структуры. Не дефект стандарта: уровень доказательности цен — модуль `confidence` существовал и не был взят; ему дописан признак, по которому ситуация узнаётся | справочник |
| 3.23 | **девятый заход внешней критики, задание инвертированное — и первые находки, которых эксплуатация не дала бы.** Проверено и починено: (1) в `kb_due.py` жила **встроенная вечная ложная тревога**: колонка входа называется «с какого числа» — дата начала ожидания, по определению прошедшая, — а скрипт считал всякую прошедшую дату просрочкой. Правильно заполненный по собственному шаблону вход давал «ожиданий с прошедшей датой: N» с первого дня, и это докладывалось владельцу как находка о его проекте. Теперь считается возраст ожидания. (2) `verified_at` изъят из списка похожих имён: справочник сам предписывает это поле паттерну зеркал — проверка наказывала за выполнение собственной рекомендации. (3) Противоречие внутри одного файла: `operations.md` объявляет правило о фиксации перед сжатием контекста неработающим и через тридцать строк его же требует. (4) Одиннадцать упоминаний удалённого линтера и ложное «убраны последние» в строке 3.22. Структурные обвинения — заморозка как громоотвод, темп выпусков как болезнь, отсутствие первичных журналов и тестов у скриптов — в разбор с владельцем, не в правку | справочник, шаблоны и скрипты |
| 3.24 | **спор о темпе выпусков превращён в счёт.** Критик обвинил стандарт в том, что темп — та самая болезнь; владелец возразил, что темп вызван диагностикой с нескольких интенсивно работавших проектов. Оба утверждения проверяемы, и в `rationale.md` добавлена разметка происхождения каждого выпуска: отчёт из эксплуатации · применение к проекту · запрос владельца · собственная идея · внешняя критика. Разрез по дням показывает, что **правы оба, но про разные дни**: вечер 2 августа — четыре выпуска из эксплуатации против девяти по соображениям, обвинение верно; 6 августа — три выпуска, все по внешним фактам, собственных идей ноль, верно возражение. Темп сам по себе ничего не говорит, говорит доля; сигнал возврата болезни — доля «собственная идея» выше доли «из эксплуатации» за период. Оговорка записана рядом: классификацию делал тот же, кто выпускал, отчёты к выпускам не приложены, и до этого таблица — заявление, а не доказательство | справочник |
| 4.0 | **первая версия с приёмочным контуром — и первая, где номер меняет первую цифру.** `scripts/test_kb.py`: одиннадцать тестов, каждый воспроизводит конкретный контрпример из отчётов и внешней критики, ни одного выдуманного. Шесть из одиннадцати при первом прогоне провалились — то есть скрипты врали ровно там, где критика и говорила. Починено: (1) объявленный путь входа, которого нет, больше не читается как «вычисляемый вход» — **опечатка в объявлении отключала единственную численную проверку молча**, fail-open вернулся через дверь, построенную для честных исключений; (2) объявление больше не вызывает ранний возврат — второй кандидат на вход находится и печатается дублем; (3) возраст входа берётся из строки «Обновлено», а не из максимума дат шапки: посторонняя свежая дата омолаживала протухший вход на годы; (4) ожидания считаются по строкам таблицы, а не по датам — две даты в строке давали двойной счёт; (5) git ищется вверх по дереву: база подпапкой внутри репозитория объявлялась «без git»; (6) явное «прогонов не было» весомее найденной рядом даты. Плюс `kb_init.py` больше **не дописывает в существующий файл проекта без `--force`** — он нарушал четвёртое правило контракта, объявляя файл пропущенным и тут же дописывая в него. `.kbconfig.yml` **удалён**: его не читал ни один скрипт, а пути и лимиты в нём были вторым источником правды рядом с файлом правил. Испорченный реестр выпусков починен: теги v3.21 и v3.22 указывали на коммит 3.23. **Стабильной эта версия не называется** — стабильность не объявляют, её переживают: ею станет та, что пройдёт объявленное окно эксплуатации без правок и на неизменных тестах | контракт, справочник, шаблоны, скрипты |
| 4.1 | **объём коммита: `git add -A` при нескольких пишущих уносит чужую незакоммиченную работу под своим сообщением.** Данные целы, история — нет: `git log` перестаёт отвечать на «когда и почему это изменилось», и восстановить ответ нельзя. Прислала сессия проекта с тремя линиями, два датированных случая за сутки; проверка нашла ещё три — **коммиты той самой сессии, которая в этот момент чинила стандарт**: под сообщением «kb-architect: редакция 4.0» уехали чужие сканы разбора телеграма и реестр источников. Владение по путям (§1) защищало от одновременной правки файла и ничего не говорило про объём коммита, а единственное место, где стандарт вообще упоминал `git add -A`, было **предписанием** — безопасным ровно при одном пишущем, хотя §1 называет нескольких нормой. Записано в git-контракт, в §1 и оговоркой в процедуре присоединения. **Выпущено вопреки объявленному часом раньше окну без правок** — по инвертированному гейту необратимого яруса: ждать подтверждения нельзя там, где сигналом успеха эксперимента является потеря. Окно начинается заново | справочник |
| 4.2 | **журнал, перенесённый разделом внутрь документа, теряет вторую колонку — и это не замечает никто.** Прислал проект с тремя линиями: восемь записей за неделю, все «сломалось»; не заметили ни три сессии, ни `kb_due.py` — он смотрел давность и не смотрел состав. Причина названа адоптером точно: **имя файла несёт половину смысла, шапка — обе, при переезде выживает имя.** Отчёт честно отнёс это к «один случай, решение автора»; счёт по остальным базам оказался резче — три, где журнал остался файлом, ведут обе колонки; один из одного, кто взял разрешённую адаптацию, потерял. Разрешение было, механизма переноса второго класса — нет: неполный механизм, а не предложение. `kb_due.py` смотрит состав и говорит, когда колонка одна; строка в шаблоне журнала; два теста. Переименование `SLOMALOS.md` отвергнуто адоптером и автором согласен: сломает всех, кто уже завёл файл. **Третье нарушение собственного окна без правок за вечер** — записано счётом, а не оправданием; основание: чинится измерительный прибор, а не правила, и неделя с испорченным прибором даёт негодное измерение  ⟦Д: если журнал ведётся разделом внутри документа, проверь, что в нём есть обе колонки — «сломалось» и «сработало»; при переезде из отдельного файла вторая теряется⟧| справочник, шаблоны, скрипты |
| 4.3 | **окно меряется интенсивностью, а не календарём** — поправка владельца, и она вскрыла противоречие внутри одного файла: очередь правок давно считает наступления триггера («триггер за период не наступал — период не засчитывается»), а разморозка считала месяцы. Два способа счёта в одном документе — то самое, что запрещает первое правило. Месяц простоя не проверяет ни одного правила; единица — сессия, которая действительно работала с базой. Пороги названы и **помечены как взятые с потолка**, считаются по уже ведущимся коммитам и записям журнала, нового учёта не заводится. То же теперь относится к заморозке кода: окно закрывается объёмом работы на неизменных тестах, а не семью днями | справочник |
| 4.4 | **шесть дефектов за один разбор одной базы, и два из них — мёртвые проверки.** (1) Запись канала правок многострочна по формату собственного шаблона, а разбор шёл построчно: отметка закрытия на строке продолжения не виделась, `marked` всегда выходил False, и ветка «неразобранное про сам вход» была **недостижима в принципе** — то есть проверка, написанная в 4.0 против fail-open, сама была fail-open и не сработала ни разу ни на одной базе. (2) Дата прогона контрольных вопросов бралась откуда попало в файле: заголовок колонки «Верный ответ (на 2026-08-02)» засчитывался как прогон, а «прогон ещё не проводился» не попадало под шаблон отрицания — эталон редактируют чаще, чем прогоняют, и чем активнее с файлом работают, тем надёжнее он выглядит проверенным. Теперь дата берётся только из журнала прогонов, а его отсутствие — находка. (3) `verify` в контракте перестал обещать больше, чем даёт: поле означает способ перепроверки, а не выполненную перепроверку. Три теста, всего шестнадцать. В очередь с условиями: шаблон входа приглашает нарушать правило 1; контракт написан в единственном числе при двух пишущих агентах; `QUESTIONS.md` как второе исключение из `valid_until`  ⟦Д: дату прогона контрольных вопросов веди отдельным разделом «Журнал прогонов»; отметки закрытия в канале правок пиши в той же записи⟧| контракт, справочник, скрипты |\n
| 4.5 | **третья редакция — опубликованная — стала видимой, и обновление можно поручить механизму.** Раньше скрипт различал две: записанную проектом и установленную на диске. Третья — что лежит в источнике — не была видна никому, и владельцу приходилось ходить по проектам со словами «перечитай скилл». Теперь `kb_paths.py` спрашивает источник **локально, без сети**: установка симлинком в репозиторий даёт ответ через git, установка файлом честно отвечает «отсюда не видно». Политика `обновление скилла: по сигналу | автоматически` в правилах проекта; авторежим тянет только вперёд и только на чистом дереве. **Обновление не применяет редакцию к базе** и говорит об этом прямо: поставить новый код и привести проект в соответствие — разные действия, а загруженные в сессию инструкции не перечитываются, горячей перезагрузки нет и обещать её нельзя. Плюс строка в шаблоне правил: расхождение редакций разбирается до ответа на вопрос владельца. **Решение владельца, не подтверждённое наблюдение** — записано так по правилу о снятии ожидания; условие отката: если авторежим хоть раз подтянет редакцию, которую проект не сможет применить в ту же сессию, режим выключается и остаётся сигнал  ⟦Д: добавь в правила строку `обновление скилла: по сигналу | автоматически` — иначе о новых редакциях никто не узнает⟧| справочник, шаблоны, скрипты |\nПравка 3.1 вскрыла собственный дефект скилла: он раздавал «готовые тексты» на десятки строк, то есть предлагал владельцу пересказывать вручную то, что уже лежит внутри. Признак этого класса ошибок общий — **если инструкцию надо копировать целиком, значит она не там, где её будут искать.**
| 4.6 | `scripts/kb_apply.py` — **разбор новой редакции против конкретной базы.** Строка «посмотри таблицу выпусков» была верной и бесполезной: таблица под сто килобайт, и владелец не должен её читать, чтобы узнать, касается ли его хоть одна строка. Скрипт достаёт выпуски между записанной редакцией и установленной и рядом с каждым ставит признак: есть ли в этой базе то, чего он касается — вход не в корне, журнал разделом, многострочные записи канала, несколько пишущих, зеркала. Признаки **помечены как грубые**: отвечают «возможно», а не «да». Главное — скрипт **ничего не применяет** и заканчивается вопросом владельцу: применение меняет соглашения проекта, а это правило 4. Отказ от части правок назван частью системы, а не отступлением | скрипты и контракт |\n
| 4.7 | **доставка отчёта: инбокс, а если его не видно — с исполнителем.** Отчёты пишут сессии на разных платформах, и часть из них облачные: локальной папки они не видят вовсе. Раньше это кончалось пересказом в чат, то есть потерей — разбирают по документам, а не по переписке. Теперь путь объявляется строкой «инбокс отчётов» в правилах проекта; не видит папку — сохраняет файл у себя и **называет владельцу, куда переложить и почему это осталось человеку**. Это применение уже записанного правила: шаг цикла, который агент выполнить не может, записывается с исполнителем, иначе считается выполненным по умолчанию  ⟦Д: добавь в правила строку `инбокс отчётов: <путь>` — иначе отчёт о дефекте некуда положить⟧| шаблоны |\nПравка 3.1 же вскрыла второй: три сборки подряд уехали владельцу под одним номером `3.0`. Отсюда правило абзацем выше — оно оплачено этим случаем, а не выведено.
| 4.8 | **запрет на секреты снят и заменён тремя режимами — по решению владельца, с уточнённым обоснованием.** Правило «секреты не хранятся никогда» смешивало несравнимое и потому обходилось: номер счёта для платежей владелец сам печатает на каждом счёте, а сертификат для налоговой физически лежит файлом. Запрет без разрешённого места — провал собственного критерия «соблюдать дешевле, чем нарушить». Теперь: идентификаторы из документов — обычные записи; доступы — место, а не значение (тот же приём, что `verify`); значение рядом с базой — только зашифрованным и только объявленным исключением. Граница переформулирована: она проходит **не по приватности репозитория, а по тому, что базу читает агент** — всё, что в базе, попадает в контекст каждой сессии и может уйти в исходящее. Плюс `SKILL.md` наконец называет **собственный источник**: пятнадцать выпусков подряд скилл не содержал адреса своего репозитория, и копия, поставленная файлом, узнать его не могла — что и наблюдалось на двух замороженных копиях 3.20, выглядевших рабочими. Плюс восьмой fail-open, найденный чтением кода: сравнение с источником шло **без `fetch`**, то есть с последним скачанным слепком, и давно не обновлявшаяся установка уверенно печатала «новее ничего не опубликовано»; теперь «не спросили» отличимо от нуля и произносится отдельно. Предложение из отчёта проверять инлайн-пути **измерено и не внесено**: 72 и 312 находок на четырёх живых базах, почти все — не поломки; запись с числами в очереди  ⟦Д: пересмотри, что считаешь секретом: счета и налоговые номера теперь обычные записи, а пароли и ключи пишутся местом («связка ключей, элемент X»), а не значением⟧| контракт, справочник, шаблоны, скрипты |
| 4.9 | **выпуск по решению владельца — «система долго проработала, объём наблюдений это оправдывает»**, и это записано как снятие ожидания, а не как выполненное условие: у 4.8 было ноль сессий работы, автор предлагал ждать. Условие отката прежнее — если правка этого выпуска даст находку про сам инструмент, счётчик обнуляется. Внутри: (1) **ветки** — `kb_due.py` считает ветки с невлитой работой и ветки без единого уникального коммита. Ветка есть второй источник состояния: работа существует, но её нет в линии, которую читает следующая сессия, и отличить пустую от рабочей глазами нельзя. Наблюдение: семь веток, пять пустых, две с невлитым, найдено вопросом владельца, а до того сессия сутки коммитила в ветку, не заметив. Правило сузилось по предложению того же проекта — ветка только под то, что заранее допускаешь выбросить, и при создании назначается её конец; про два агента в одном дереве сказано прямо, что ветка там вредна. Замер перед внесением: пять живых баз, одна находка. (2) **`kb_apply.py` наконец назван тем сигналом, ради которого написан** — с 4.6 инструмент существовал, а строка «редакции разошлись» отправляла читать стокилобайтную таблицу; проект, живший на 3.20 при установленной 4.8, видел этот совет каждую сессию и не выполнял его ни разу, потому что выполнение стоило дорого, а невыполнение ничего. Тот же класс, что `kb_standard_version` без шага создания, только на два месяца позже. (3) **семь триггеров отчёта переехали в шаблон**: они жили у автора и проекты их не видели — механизм, решающий, когда писать, был недоступен тем, кто пишет. Седьмой добавлен новый — дорогая операция, повторяющаяся из-за того, что результат некуда положить; первые шесть покрывали только неправду, поэтому самое дорогое наблюдение отчёта пришлось просить руками. (4) **раздел «как удешевлять»** в измерении: не делать дважды; читать дешёвым до дорогого (текстовый слой → локальное распознавание → глаза агента); субагент возвращает адреса и цитаты, а не пересказ, иначе это новый непроверенный источник за ваши же деньги; что агент обязан смотреть сам — отметки, печати, привязка числа к колонке, рукописное. Класс «у утверждения не хранится адрес источника» — пять наблюдений одного отчёта — **в очередь: недостаёт решения владельца, а не доказательств** | справочник, шаблоны, скрипты |
| 4.10 | **замер ресурсоёмкости самого скилла — и первый потолок, который стандарт поставил себе.** Поводом был вопрос владельца: во что обходится проекту применение этого скилла. Померено токенизатором, 6,2 байта на токен на этом тексте. Обязательная страница — 5 392 токена, платится каждой сессией, где скилл сработал; `rationale.md` — 16 300, и **80% его составляла таблица выпусков**, то есть любая сессия, открывшая «почему правила такие», платила за всю историю заодно. Таблица вынесена в `references/releases.md` с пометкой «читается скриптом, а не глазами»; `rationale.md` похудел впятеро, до 3,4 тыс. Попутно выяснилось, что вчерашняя правка 4.9 была не про удобство, а про цену: `kb_apply.py` отвечает на вопрос «что изменилось» за 1 500 токенов вместо 16 300 — **выигрыш в десять раз, сделанный вслепую**. Отсюда принцип, записанный в измерение: инструмент, отвечающий на вопрос, дешевле документа, содержащего ответ, и новая способность сначала пробуется скриптом. У каждой строки меню справочника теперь стоит цена: решение «читать или нет» принимается до чтения, иначе принимается всегда в пользу чтения. И главное: **`SKILL.md` получил потолок 40 КБ с проверкой в сборке** — стандарт требовал потолок входа от чужих баз и не имел его у себя, а страница росла монотонно, потому что добавить всегда легче, чем вычесть. Потолок проверен отрицательным прогоном: сборка падает | справочник, сборка |
| 4.11 | **у раздела «как удешевлять» не было границы — а он уже работал на всех установках.** Написан шестью часами раньше, в 4.9, и в таком виде читался как «оптимизируй агрессивно». Дефект несимметричный: экономия измерима сразу, ущерб от ненайденного факта не измерим вовсе, поэтому оптимизация всегда выглядит выигрышной в моменте, а проверить её задним числом нечем. Граница названа владельцем и записана дословно: **оптимизировать доставку, а не доступ** — убрать из загрузки ненужное безопасно, потому что множество доступных данных не меняется; отговаривать агента читать опасно, потому что правило «сначала поиск» обязано уступать, как только поиск не ответил. Плюс признак пережима, по которому это ловится механически: агент выполнил процедуру и **не нашёл того, что в базе есть** — это повод снять ограничение немедленно, а не обсуждать, и повод для отчёта по триггеру 1, потому что процедура при этом отработала «чисто». Выпуск на одну оговорку: правка чинит уже отгруженный текст, а не добавляет способность | справочник |
| 4.12 | **счётчик канала правок горел неделю не падая — мой сигнал, мой дефект.** Прислал медицинский архив. Сначала я решил, что причина в построчном разборе многострочных записей — том самом, что чинился в 4.4, — и **проверил счётом: по строкам 29, по записям 27**. Гипотеза объясняла два случая из двадцати девяти, то есть была неверна, и хорошо, что померена. Настоящая причина: отметка закрытия — **доля, а не «есть или нет»**. Код различал два состояния (ноль отметок — «не отслеживается»; хоть одна — считать все непомеченные неразобранными), а живая база живёт между: 239 записей и 59 отметок, 25%. Проставить отметку — та же дешёвая-дорогая пара, что и внесение факта в канон, и её пропускают. Теперь считается доля; ниже порога (взят с потолка, половина) печатается справка с числом, а не находка. Замер после правки: три базы дают 36%, 42%, 48% — то есть **ветка тревоги сегодня не срабатывает нигде, и это сказано вслух**, а не спрятано. Вторая правка, на обязательной странице: **запись в канале факт не закрывает, а открывает.** «Источник не править — это отдельная работа» читалось как «на этом всё», и в присланном отчёте четыре из восьми расхождений базы оказались одним этим отказом — самый дорогой случай: единственное свидетельство о прививке ребёнка лежало в канале, в каноне его не было, и база отвечала «сведений нет». Закрывает факт внесение в канон, названное адресом; дописать исход в ту же запись — не правка append-only, а её продолжение. Плюс в измерение записаны числа, впервые доказывающие, что контрольные вопросы и скрипты ловят разное: вопросы нашли восемь расхождений, скрипты — ни одного из них. В очередь: класс, где доказательство есть, а способа перепроверить не существует  ⟦Д: закрывая запись в канале правок, указывай адрес: `✔ закрыто ДАТА, внесено в ПУТЬ`. Отметка без адреса больше не считается закрытием⟧| контракт, справочник, скрипты |
| 4.13 | **внешний аудит GPT-5.6 нашёл, что приёмочный контур врёт, и четыре fail-open в отгруженном коде.** Все пять проверены автором на фикстурах до починки; одна оказалась хуже заявленного. (1) **Контур из двадцати зелёных тестов утверждал больше, чем проверял:** при подмене запуска на пустой вывод «ок» печатали **восемь тестов из двадцати** — отрицательное условие «плохой фразы нет» выполняется и на пустоте. `run()` теперь несёт код возврата, `check()` валит тест до содержательных утверждений; мутационный прогон даёт 19 провалов из 20 вместо 8 успехов. Порог поломки — код ≥ 2: единица у `kb_check.py` штатная. Чинилось **первым**, потому что остальным чинить было бы нечем — прибор врал. (2) **Ошибка git становилась «дерево чистое».** Обёртка отдавала пустую строку и при исключении, и при ненулевом коде; пустые `dirty` и `ahead` складывались в зелёную строку. Воспроизведено подложенным `git`, всегда выходящим с кодом 2. Теперь неудача — `None`, и она печатается как НЕ ПРОВЕРЕНО; отсутствие upstream отделено от отказа команды. (3) **Объявленный и отсутствующий журнал** лежал в «в порядке», **объявленный и отсутствующий канал правок не давал ни одной строки** — для этого исхода не было ветки вовсе. Оба стали находками. (4) **Вложенный подзаголовок обнулял ожидания:** локальная `section()` обрывала раздел на любом заголовке, включая `###`; правильный парсер с уровнями всё это время лежал рядом в `kb_paths` и не использовался. (5) Шаблон канала учил закрывать записи без адреса, тогда как контракт 4.12 требует адрес — выровнен. (6) Из `patterns.md` убрано обещание «файл без `valid_until` в типе `snapshot` линтер отклоняет»: такой проверки в коде нет и не было, на фикстуре файл проходил «чисто». Четыре новых теста, всего двадцать четыре. **Опровергнут собственный вывод реестра** «отчёты дают больше находок на единицу, чем ревью»: одно ревью дало пять подтверждённых fail-open, четыре новых | справочник, шаблоны, скрипты |
| 4.14 | **второй внешний аудит: справочник и шаблоны оказались параллельным нормативным слоем.** Периметр — восемь справочных файлов и все семнадцать шаблонов, которых первый аудит не смотрел. Девятнадцать находок; в этот выпуск взяты подтверждённые противоречия, то есть вычитание и выравнивание. **Первым — собственный провал метода:** правило о секретах, переписанное в 4.8, всё это время жило в старой категорической форме в `collaboration.md`, потому что автор искал его `grep -i` при `LC_CTYPE=C`, где регистр кириллицы не сворачивается — поиск возвращал ноль и читался как «этого здесь нет». Пять выпусков со сказанным вслух «починено» при починке на две трети; тот же класс, что ловим у скрипта, только в инструменте проверяющего. Дальше: правило «почувствуешь, что контекст на исходе, — зафиксируй» **отменено в `operations.md` как опирающееся на самооценку** и при этом продолжало жить в `patterns.md`, `collaboration.md` и в копируемом шаблоне правил — три копии отменённого, выровнены по канону «по объёму несохранённого». Шаблон знания предлагал `review_by` вместо `valid_until` — под другим именем проверка поля не видит, а «имя поля — часть поля» записано в самом контракте с 4.7. Четыре места ссылались на конфиг, удалённый в 4.0, в том числе строкой плана перестройки «из конфига». В процедуре перестройки не существовало шага 2: нумерация шла 1 → 3, то есть нельзя было сказать «остановились на шаге N». Команды процедур звали `python3 scripts/...` относительно каталога проекта, где скриптов нет; восемь мест приведены к каноническому виду, который шаблон правил и так использовал. Не взято в этот выпуск и вынесено владельцу: две разошедшиеся процедуры перестройки, gate перед первым push сырья, зеркала, требующие несуществующего синхронизатора  ⟦Д: замени в правилах строку про фиксацию «когда чувствуешь, что контекст на исходе» на «незакоммиченного больше одного содержательного блока — коммить»; и проверь, нет ли в базе поля `review_by` там, где должно стоять `valid_until`⟧| справочник, шаблоны |
| 4.15 | **вторая копия процедур перестройки и запуска удалена — решение владельца по второму аудиту.** В `operations.md` те же две работы были изложены второй раз и успели разойтись с оригиналом: версия оттуда начинала изменения **без резервной ветки и тега**, тогда как канон требует их до первой правки, и строила план «из конфига», удалённого в 4.0. Сессия открывала ту копию, на которую натыкалась первой. Это первое правило контракта, нарушенное самим справочником: два независимо поддерживаемых источника одного знания разошлись, и узнать об этом было нельзя. Удалено 15 589 байт, на их месте указатель; `operations.md` похудел с 44 968 до 31 235. Владелец отдельно отклонил вторую находку аудита — обязательный вопрос про секреты перед первой заливкой сырья: все репозитории приватные, среда доверенная, а случай публичной базы оператор объявляет явно. Взамен усилено то, о чём он попросил: дисциплина коммитов теперь стоит строкой в копируемом шаблоне правил, то есть попадает в каждый проект, а не живёт в справочнике | справочник |
| 4.16 | **профиль зеркал изъят целиком — крупнейшее удаление за всё время, решение владельца.** Зеркало было конспектом внешней системы в базе, перенесённым из прежней нормативной системы вместе с паспортами и профилями; спросить, нужно ли, забыли. Второй аудит показал, что выполнить его нельзя: отметку синхронизации «обязана ставить программа, а не человек», а программы в пакете нет и завести её ни одна процедура не велит. Замер добил: **92 упоминания в четырнадцати файлах пакета при нуле файлов-зеркал на четырёх живых базах** — два найденных совпадения оказались старым ревью и заметкой о соседнем проекте. Удалено 14 993 байта: два шаблона, профиль в `profiles.md`, модуль зеркал кода, механика синхронизации в `collaboration.md` (2.2–2.7), асимметрия удаления в `patterns.md`, поля `verified_against`/`synced_from`/`synced_at`/`mirrors` из таблиц. **Что осталось от идеи — одна фраза владельца, и она сильнее аппарата:** записывая результат события, записывай и где его можно подтвердить. «Декларация подана — подтверждение в личном кабинете налоговой». Это то же поле `verify`, сказанное прямо: адрес нужен не когда его требует проверка, а всегда, когда утверждение о внешнем мире попадает в базу. Тем самым закрыт и класс из отчёта проекта ВНЖ — пять наблюдений о том, что у утверждения не хранится адрес источника, — причём вычитанием, а не новой обязанностью. Распознавание `type: mirror` в `kb_check.py` сохранено намеренно: снятая способность не должна оборачиваться потоком ложных находок у того, кто ею успел воспользоваться  ⟦Д: если в базе есть файлы с `type: mirror` — переведи их на обычную запись: результат события плюс адрес, где его можно подтвердить⟧| контракт, справочник, шаблоны |
| 4.17 | **«обновись» стало одной командой — до этого обновление было домашним заданием на полчаса.** Владелец сформулировал претензию точно: соседний проект должен получать найденные улучшения и чинить свои старые ошибки понятным механизмом, а не длинным промптом, смысла которого владелец не понимает. Так и было: `kb_apply.py` вываливал **семнадцать статей** и предлагал по каждой решить, брать или нет. Разбор показал, что вопрос стоял неверно: **правки инструментов и справочника приезжают сами** вместе с установкой и от базы ничего не требуют; действия требуют только те выпуски, что меняли **обязанности проекта**. Таких из семнадцати оказалось пять. В таблице выпусков появилась машинная метка `⟦Д: …⟧` — одна фраза о том, что обязан сделать проект; проставлена у девяти выпусков, начиная с 3.18. `kb_apply.py` печатает теперь пронумерованный список дел и порядок из четырёх шагов, а не историю. Смысл слова «обновись» положен в **шаблон правил проекта** — то есть уезжает в каждую базу и не требует от владельца ничего, кроме одного слова. Проверено на живом проекте, отставшем на семнадцать выпусков: было семнадцать статей, стало пять дел | контракт, справочник, шаблоны, скрипты |
| 4.18 | **автообновление, написанное в 4.5, покрывало одну установку из трёх — и молчало об этом.** Владелец спросил, почему приходится просить файл руками, если автообновление прописано. Замер на живой машине: `pull_skill()` делает `git pull` в папке скилла, а такая папка есть репозиторий только у симлинка Claude Code; копия для Codex git не имеет и отстаёт молча, скилл уровня приложения файловой системы не имеет вовсе. Две трети установок устаревали, и снаружи это было неотличимо от свежих — тот же отказ, что ловим везде, только в механизме доставки. `scripts/kb_update.py`: показывает все установки на машине рядом с источником, `--сделать` подтягивает источник и переписывает отставшие копии. То, что машина сделать не может, **названо вслух**: пакет для приложения отдаётся владельцу руками, и это шаг с исполнителем, а не выполненный по умолчанию. Проверено мутацией: в копию Codex подложена редакция 4.13, скрипт назвал её отставшей и обновил до 4.17. Политику `обновление скилла` объявили не все проекты — двое «автоматически», один «вручную», один не объявил | справочник, скрипты |
| 4.19 | **два мозга — один канон, а сообщение не зависит от транспорта.** Реальный параллельный прогон подтвердил: одна ветка не изолирует двух писателей в одном checkout. Справочник теперь различает последовательную работу в единственном дереве и параллельную запись в отдельных worktree/ветках; постоянная вторая редактируемая копия прямо названа вторым каноном. Inbox разделяет документы владельца и адресованные сообщения агентов; новый шаблон сохраняет один конверт при передаче файлом, нативным каналом или через владельца и запрещает статусные сообщения без дельты. Выровнены подтверждённые противоречия шаблонов: `verify`, условная диагностика, нейтральные заголовки handover, явное разрешение на подробный defect-report. Обещания `kb_due.py` сужены до реально измеряемого: дата последней записи не выдаётся за дату разбора, число файлов — не за смысловой блок. Замороженный контракт не менялся; отдельный разбор заморозки проведён по накопленным отчётам  ⟦В: если проектом пользуются Claude и Codex, решить, принять ли один checkout при последовательной работе, отдельные worktree при параллельной записи, единый конверт сообщений и запрет статусной болтовни без дельты⟧| справочник, шаблоны, метаданные |
Оба дефекта правки 3.2 нашлись **при первом применении скилла к его собственному репозиторию** — `kb_init.py` был запущен на репозитории kb-architect, и оба всплыли в первые минуты. Приём общий и оказался за сессию самым результативным: **артефакт прогоняется через собственные требования**. Он же ловил, что скилл требует git и сам не в git; что скилл запрещает два конкурирующих входа и содержит два формата входного файла; что скилл запрещает пересказывать базу и предлагает копировать себя в чат сорока строками.

| 4.20 | **установленная копия была byte-for-byte верной и всё равно не могла пройти собственный тест.** Первичный отчёт из приватного проекта воспроизведён на 4.19: source 30/30, installed 29/30; единственный тест `kb_update.py` молча заимствовал окружающий `.git`. Теперь тест создаёт собственный временный repo-backed source fixture и одинаково проходит из checkout и установленной копии. Updater прямо говорит, что отсутствие `.git` у installed-copy штатно и не означает повреждённую версию; поиск `.skill` понимает и публичный корень, и private-lab `package/`. Ядро не менялось | скрипты, тесты |
| 4.21 | **проект переезжает в общее поле как принятая система, а не как папка.** По прямому требованию владельца добавлена одна нейтральная процедура для Claude и Codex: команда «перенеси себя в общее поле» проводит preflight, backup, проверку Git/worktree/nested repo, один канон правил под `AGENTS.md` и `CLAUDE.md`, перенос без постоянной копии, обновление активных путей и две независимые приёмки свежими сессиями. Сам `mv` не считается готовностью. Одновременно воспроизведён дефект обновления: 4.19 установился, но `kb_apply.py` печатал «дел нет», потому что видел только новые обязанности и молчал о новых опциональных способностях; ни один проект не узнал о межагентской формализации. Теперь таблица различает `⟦Д: …⟧` и `⟦В: …⟧`, а проект записывает `принято / отклонено / отложено`. Сервисный контур берёт стабильную редакцию только из PUBLIC GitHub: development-checkout не канал установки; updater использует staging/backup/tests и превращает старый симлинк в управляемую копию. Добавлена нейтральная модель ролей знания — источник, наблюдение, утверждение, интерпретация, решение, вопрос и производное представление. Для нового проекта это адаптируемая отправная точка, для исторического — read-only чек-лист; предметную онтологию и каталоги скилл не навязывает. Отдельная сборка мусора уменьшает активное поле только после проверки канона, ссылок, доказательной роли, предметного срока хранения и восстановления; квитанция не удаляется лишь потому, что её редко читают. Первый случай перестройки по инициативе владельца не считается успехом ещё не применённого сигнала. Контракт не менялся  ⟦В: если владелец хочет общий корень Claude и Codex, применить `references/move-project.md`; до двух приёмок не считать проект совместимым с обеими системами⟧  ⟦В: решить, принять ли сервисный контур из `references/service-layer.md`: доставка стабильного выпуска из PUBLIC GitHub, применение обновления, defect-report по триггерам и сигнал о дорогой структуре⟧  ⟦В: решить, нужна ли проекту модель ролей из `references/knowledge-roles.md` как стартовая схема или аудит происхождения, фактов, интерпретаций и решений⟧  ⟦В: решить, принять ли процедуру сборки мусора из `references/garbage-collection.md`; удаление требует retention authority, точного списка и проверяемого восстановления⟧| справочник, шаблоны, скрипты, тесты |

| 4.22 | **совпадающий номер версии скрывал неправильный тип установки.** Живой post-release прогон 4.21 увидел Claude Code как симлинк в development lab и обещал заменить его при `--сделать`, но затем сравнил только `4.21 == 4.21`, объявил установку обычной копией и ничего не сделал. Это нарушало новый стабильный канал при зелёных тестах. Updater теперь проверяет тип отдельно: симлинк заменяется управляемой PUBLIC-копией даже при том же номере; прежняя ссылка сохраняется в backup. Регрессия создаёт такой симлинк и проверяет физический каталог после замены. Контракт и проектные обязанности не менялись | скрипты, тесты |
| 4.23 | **публичное описание стало двуязычным и прямо называет обе платформы.** GitHub About говорил только “knowledge-base contract and agent handoff toolkit”, а README начинался по-русски и одной английской сноской о русскоязычном содержимом. Вверху README теперь симметричные RU/EN формулировки и английский обзор без второй копии контракта: один stable release для Claude и Codex, один проектный канон, последовательный checkout и worktree для параллельных писателей. Одновременно уточнено честное ограничение поиска: встроенный lookup лексический, его пустая выдача не доказывает отсутствия; после измеренных промахов проект может принять `semantic_retrieval` как производный vector/search слой, который возвращает кандидатов и никогда не становится каноном. Export-регрессия требует оба языка и обе платформы. Ядро, инструменты и обязанности проектов не менялись | публичная документация, справочник, тесты |
| 4.24 | **первый полный цикл реорганизации зрелого проекта проверил справочник на крупной живой базе.** Подтверждены и исправлены три универсальные неоднозначности: перестройка теперь до описи называет устойчивый объект и назначение, чтобы не воспроизводить историческую ось папок; активный потребитель внутреннего пути отделён от неизменяемого исторического output, который сохраняют, но не превращают в вечный запрет на изменение живого канона; backup переноса разложен на единственный рабочий checkout, remote-recovery, bundle refs и snapshot данных вне Git — резервные слои не являются вторым репозиторием. В `collaboration.md` добавлен опциональный паттерн размещения доменного скилла по области действия: repo-local для одного проекта, управляемая общая доставка для нескольких, fail-closed при недоступном обязательном каноне. Свежая сессия и первый реальный цикл уже покрыты `move-project.md`; платформенная project-регистрация не стала новым правилом. Предложение объявлять scope каждого Inbox-лота оставлено в очереди: существующее правило уже требует один связный содержательный блок, а полезность дополнительной декларации пока не доказана. Три регрессии фиксируют новые различия. Замороженный контракт не менялся | справочник, очередь, тесты |

### Чем вызван каждый выпуск — и почему это считается, а не обсуждается

Внешняя критика обвинила стандарт в том, что темп выпусков — та самая болезнь, от которой он себя лечил: «артефакт, конвертирующий любую критику в новую версию». Владелец возразил: темп — следствие того, что несколько проектов работали интенсивно и прислали диагностику.

Оба утверждения проверяемы, и спорить о них незачем. Признак, который их различает: **что было на входе выпуска — наблюдение из работы или соображение.**

| Происхождение | Выпуски | Сколько |
|---|---|---|
| **отчёт о дефекте из эксплуатации** | 3.6, 3.16, 3.18, 3.20, 3.21, 3.22 | 6 |
| **применение к живому проекту вскрыло** | 3.7, 3.19 | 2 |
| запрос или указание владельца | 3.13, 3.14, 3.15, 3.17 | 4 |
| собственная идея или ревизия | 3.8, 3.9, 3.10, 3.12 | 4 |
| внешняя критика · безопасность публикации | 3.23, 3.11 | 2 |
| сборка и подготовка к публикации | 3.0–3.5 | 6 |

Разрез по дням отвечает на спор точнее, чем общий счёт:

- **вечер 2 августа, тринадцать выпусков.** Из эксплуатации — четыре. Остальные девять: четыре собственные идеи, четыре запроса владельца, один про безопасность. Обвинение критика для этого вечера **верно**: большинство правок вызвано соображениями, а не работой, и там же стоит признание «сделано вопреки собственной рекомендации остановиться» — через тринадцать минут после самой рекомендации.
- **6 августа, три выпуска.** Два отчёта с другой платформы и один заход критики, где четыре находки из четырёх проверены механически, а структурные обвинения не приняты, а записаны. Собственных идей — ноль. Возражение владельца для этого дня **верно**.

Вывод не в чью-то пользу: **происхождение выпусков сместилось от собственных идей к внешним фактам, и это ровно то, чего добивалась заморозка.** Но сам темп ни о чём не говорит — говорит доля.

**Сигнал, по которому это станет болезнью снова:** доля «собственная идея» за период выше доли «из эксплуатации». Считается по этой таблице, а не по ощущению; спорить не нужно.

**Чего таблица не доказывает.** Классификация сделана тем же, кто выпускал. Отчёты существуют, но лежат в проектах и в переписке, а не приложены к выпускам, — поэтому «из эксплуатации» здесь пока верится на слово. Пока сырьё не приложено, эта таблица — заявление, а не доказательство, и обвинение критика «первичных фактов нет» ею не закрыто.

### Бенефициар правила

У спорных правил есть тот, чьё удобство они покупают, и это стоит называть.

| Правило | Кому удобно |
|---|---|
| Мельче файлы, резче ярлыки, лимит на размер | агенту: грузит точечно |
| Толще главы, меньше файлов | человеку: держит карту в голове |
| Паспорт и линтер | агенту и аудиту; человеку — накладная |
| Указатель в правилах, подтверждение при входе | человеку: проверяет, что база сработала |
| `CORRECTIONS.md` | агенту: снимает цену с неудобной информации |
| Спека одним файлом сверх лимита | человеку: спеку читают целиком или не читают вовсе |

Когда правила конфликтуют, вопрос решается не лимитом, а тем, чьё удобство сейчас покупается. Названный бенефициар превращает отступление в осознанную сделку, а не в нарушение.

---

Отвергнутое всеми тремя вылетело из ядра. Принятое всеми тремя осталось. Подошедшее одному-двум стало модулем.

---
